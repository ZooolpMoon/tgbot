{
  "name": "tgbot",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Telegram AI Bot on Cloudflare Workers",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy -c wrangler.production.toml",
    "tail": "wrangler tail",
    "backup": "node scripts/backup.mjs",
    "backup:local": "node scripts/backup.mjs --local",
    "backup:config": "node scripts/backup-config.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
