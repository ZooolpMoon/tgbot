# 📜 更新日志

> 当前版本 **v3.10.0** · 变更记录（本页）· [README](README.md) · [文档索引](docs/README.md) · 许可 [GPL-3.0-or-later](LICENSE)

按版本**倒序**排列，最新的在最上面。带 ❗ 的是**破坏性变更**，升级前先看那一节的「升级提示」。

## 📑 版本速览

| 版本 | 一句话 | 性质 |
|------|--------|------|
| **v3.10.0** | 六项新能力：自动反垃圾 / 每日群报 / 群内抽奖 / 长期记忆 / 用量面板 / Web 后台 | ✨ 新增 |
| **v3.9.1** | 修「/welcome 与功能开关不同步」：群聊层改成真正的群级作用域 | 🐛 修复 |
| **v3.9.0** | 入群欢迎与验证 / AI 引用消息 / 知识库重排序 + 修 RAG 与资金两处隐患 | ✨ 新增 |
| **v3.8.0** | 审查遗留项清零：退避截断 / 权限判断不一致 / 商城三处 / RAG 裁剪与缓存 / 备份 | 🐛 修复 |
| **v3.7.0** | 审查驱动的集中修复：堵刷分赔率、堵管理员被封禁、堵重复执法 + cron 拆分与索引 | 🐛 修复 |
| **v3.6.1** | 修 21 点资金安全：连点「停牌」可重复领奖（v3.5.0 引入） | 🐛 修复 |
| **v3.6.0** | 新游戏 🔴⚫ 轮盘赌：欧洲轮盘 37 格，押红黑 / 单双 / 大小 / 三打 | ✨ 新增 |
| **v3.5.0** | 新游戏 🃏 21 点：跟 AI 庄家对赌，可要牌 / 停牌 / 双倍 | ✨ 新增 |
| **v3.4.0** | Webhook 自愈巡检：地址被清空（换 Token 后常见）会自动补回并告警 | 🐛 修复 |
| **v3.3.0** | 商城有了「🎒 我的背包」（买到的虚拟物品自己取用）+ 已完成订单退款 | ✨ 新增 |
| **v3.2.2** | 群标签：提前拦下群主 / 管理员（`CHAT_CREATOR_REQUIRED`），说清为什么用不了 | 🐛 修复 |
| **v3.2.1** | `/addpoints` 支持用户 ID / @用户名，报错说清该怎么写 | 🐛 修复 |
| **v3.2.0** | 商城支持「自动发放」商品，内置 🏷️ 自定义群组标签（500 积分） | ✨ 新增 |
| **v3.1.3** | 管理员指令的可见性对齐多管理员模型：菜单与 `/help` 都按能力裁 | 🔧 优化 |
| **v3.1.2** | 预警对管理员一视同仁 · 修「每日概况每 2 分钟弹一次」 · 时间统一按北京时间展示 | 🐛 修复 |
| **v3.1.1** | 操作日志补齐中文标签 + 新增「👑 管理员 / 🗑️ 自动删除」筛选 | 🐛 修复 |
| **v3.1.0** | 消息自动删除新增「🌍 全局兜底」：到点删除机器人所有消息（30 分钟 / 1 小时） | ✨ 新增 |
| **v3.0.1** | 修 AI「查数据」不灵：改成确定性预取，工具名不再泄漏给用户 | 🐛 修复 |
| **v3.0.0** | 多管理员与角色权限 · 统一配置模型 · AI 工具调用 · 积分转账与抽奖 · 缓存与异常告警 | ❗ 大版本 |
| **v2.9.0** | 下线「每日任务」功能 | ❗ 破坏性变更 |
| **v2.8.2** | 修复「添加任务」点不动：引导文案里的 `<兑换码>` 未转义 | 🐛 修复 |
| **v2.8.1** | 管理员不能封禁自己；引导会话互斥；一批转义与健壮性修复 | 🐛 修复 |
| **v2.8.0** | 处置只认 `/指令`；消息自动删除可按类型设置 | ❗ 破坏性变更 |
| **v2.7.1** | 知识库回答模式（hybrid）+ 相关度门槛 | 🐛 修复 |
| **v2.7.0** | 处置到期通知 · 申诉通道 · 静默预警 · 用户详情 · 群规版本 · 知识库增强 | ✨ 新增 |
| **v2.6.0** | 群规引导式编辑 · 输入框命令菜单 · 成员举报联动 | ✨ 新增 |
| **v2.5.0** | 群规执法（封禁 / 踢出 / 禁言，含理由校验与确认卡片） | ✨ 新增 |
| **v2.4.0** | 知识库（RAG） · 群组两级浏览 · 封禁名单 | ✨ 新增 |
| **v2.3.1** | 用户管理收敛为二级菜单 | 🔧 优化 |
| **v2.3.0** | 统一菜单排版工具；修一批会话与并发问题 | 🔧 优化 |
| **v2.2.0** | 恢复三级功能开关 + 菜单两列化 | ✨ 新增 |
| **v2.1.1** | Schema 版本标记（冷启动跳过建表） | 🔧 优化 |
| **v2.1.0** | 每日任务可引导式增删改（v2.9.0 已下线） | ✨ 新增 |
| **v2.0.0** | 声明式命令注册表 · 功能开关 · 商品限购 | 🏗️ 架构 |
| **v1.3.1** | 商城只保留虚拟物品与服务，去掉发货环节 | ❗ 变更 |
| **v1.3.0** | 兑换码 · 下单备注 · 定时任务 · 测试套件 | ✨ 新增 |
| **v1.2.0** | 引导式编辑商品 · 自助取消订单 · 翻页与排行榜 | ✨ 新增 |
| **v1.1.0** | 引导式添加商品 · webhook 校验 · 商品分页 | ✨ 新增 |
| **v1.0.0** | 首个版本上线 Cloudflare Workers | 🎉 首发 |

---

---

---

## v3.10.0 · 2026-09-19

> ✨ 六项新能力一次落地：机器人第一次能**自己**按住刷屏、能总结群里聊了什么、
> 能记住老用户、能量化自己的开销，并且有了 Telegram 之外的第二个管理入口。

### ✨ 新增

- **🧹 自动反垃圾**（`/automod`，**默认关闭**）
  - 与「群规执法」分工明确：执法是**人**发现违规后下指令，反垃圾是机器人按**行为特征**先兜一层
  - 三条规则：刷屏（10 秒 6 条 / 单条 1200 字以上）、重复发言（60 秒内同一内容 3 次以上）、
    新成员沙盒（入群 30 分钟内禁发链接与转发）
  - 默认只**删消息 + 群内提示**；管理员可改成「递进禁言」（第 2 次起 10 / 60 / 1440 分钟）
  - **只看行为，不解析内容语义** —— 沿用「不猜自然语言意图」的既有底线
  - **自己人一律豁免**：群主、本群管理员、机器人管理员与执法员都不会被自动处置
  - 计数走 isolate 内存滑动窗口，**只有真正触发时才写 D1**；配置有 30 秒缓存，
    所以日常聊天不产生任何额外查询
- **📰 每日群报**（`/summary`，**默认关闭**）
  - 日报时段自动生成「昨天群里聊了什么」推给管理员，也可随时 `/summary` 手动生成
  - ⚠️ `chat_history` 存的是**与 AI 的对话**，不含群成员之间的聊天，所以想总结群聊
    必须另记一份流水 —— 这也是它默认关闭的原因：**开启等于管理员同意记录群成员发言**
  - 只记**文本消息**的前 300 字，图片 / 语音 / 文件 / 指令一律不记，只留最近 7 天
  - 统计（消息量 / 活跃人数 / 发言排行 / 新成员 / 自动处置次数）走 SQL 直接算，
    **摘要才调模型**；模型失败时降级成「只有统计」，不会整份日报没有
  - 提示词里明确要求不复述隐私信息（手机号、住址、证件号）
- **🎁 群内抽奖**（`/giveaway`、`/giveaway_end`、`/giveaway_cancel`）
  - 管理员发起 → 成员点按钮报名 → 到点（每 2 分钟的 cron）自动开奖，或管理员立即开奖
  - 参数形如 `/giveaway 100 3 30 周末大抽奖`（积分 / 人数 / 分钟 / 标题，后三项可省）
  - **开奖只认 `status: open → drawn` 的原子更新**：到点 cron 与手动开奖同时发生也只发一次奖
    （v3.6.1 的 21 点事故教训：不能拿 SELECT 的结果当凭据）
  - 报名用 `INSERT ... ON CONFLICT DO NOTHING`，连点只算一次；中奖积分复用 `refundPoint`，
    保证「加分必有流水」
  - 命令名刻意避开 `/draw` —— 那是 `/lottery`（每日抽奖）的别名
- **🧠 长期记忆**（默认开启）
  - 上下文窗口只有 10 条 / 6000 字符，超出的旧对话原来**直接丢掉**，于是老用户回来
    机器人照样问「你是谁」。现在被裁掉的历史会攒进缓冲，够 8 条就压成一段「用户印象」
  - 画像只记稳定的信息（称呼、长期关注的话题、偏好），明确要求不记隐私、不编造
  - 注入时标注为**背景 + 不可信素材**：不许复述、不许执行其中的指令
  - 模型没返回内容时**保留缓冲**，下次再试 —— 不能把用户的记忆白丢一次
  - `/clear`、`/clearmem` 与后台的「清空记忆」都会连画像一起清掉
- **📊 用量与成本面板**（`/usage`）
  - AI 调用次数与失败率、回退到备用模型的次数、各模型调用分布、群消息量、知识库检索量
  - 埋点是内存累加 + 请求结束时一次 batch UPSERT：一天一个指标一行，**写入量与消息量无关**
  - 统计是**近似值**（多 isolate 各算各的），用来观察趋势，不作计费依据
- **🖥️ Web 管理后台**（`/web`）
  - 概览数字、用户搜索、封禁 / 解封、改积分、翻审计日志、导出用户 CSV
  - 登录**不用 Telegram Login Widget**（那要配 BotFather 域名，换域名就登不进去），
    改成管理员在 Telegram 里发 `/web` 换一条 5 分钟有效的一次性链接
  - 会话是 HMAC 签名的 HttpOnly + SameSite=Strict cookie，并且**每次请求都重新查一次角色** ——
    cookie 有效不代表还是管理员，撤权立即生效
  - 写操作全部复用指令侧的既有函数（`banUserById` / `unbanUserById` / `resolvePointTarget`），
    所以「不能封禁机器人管理员」这类兜底在 Web 上同样成立

### 🔧 优化

- **功能开关面板加分页**：开关从 11 项涨到 15 项，一页铺满会顶破「整菜单 ≤ 8 行」的约定。
  现在每页 6 项（3 行）+ 翻页 + 返回，最多 6 行；按钮里回填的作用域 token 仍是不带页码的干净值
- **模型调用抽成 `services/ai-model.js`**：日报与长期记忆都要调模型，
  原先只有 `handlers/ai.js` 里有回退链；现在三个调用方共用一份，用量统计也顺带统一

### 🧪 测试

- 测试 448 → **489 个**：新增自动反垃圾、群报、抽奖、长期记忆、用量统计、Web 后台六组守卫
  - 反垃圾：窗口滑出、超长、重复归一化、新成员链接（含 `text_link` 实体）、
    递进档位、**开关关闭时不动**、**冷却期内不重复处置**、**owner 与群主豁免**、指令不算刷屏
  - 抽奖：参数解析（含越界拦截）、无放回抽取、报名幂等、
    **重复开奖不会再发一份奖**（积分与流水都要对得上）、跨群报名拦截
  - 长期记忆：缓冲攒够才压缩、模型空回复时保留缓冲、开关关闭不压缩、坏数据不卡死
  - 群报：默认不记录、只记文本、超预算保留最近的、模型失败降级成统计
  - Web 后台：令牌一次性、过期令牌失效、伪造 cookie 被拒、**非管理员换到 cookie 也进不去**、
    执法员改不了积分
- `test-helpers/d1.mjs` 的 `batch` 补上「SELECT 走 all()」：真实 D1 的 batch 对查询返回
  results，替身原先一律走 run()，导致任何「batch 里带 SELECT」的代码在测试里读不到行

---

## v3.9.1 · 2026-09-19

> 🏠 修「/welcome 与功能开关不同步」：群级功能开关以前根本没写到同一个键上。

### 🐛 修复

- **功能开关的群聊层改成真正的「群级」作用域**（v3.9.0 的 /welcome 正好踩在这个坑上）：
  - **现象**：在「功能开关 → 群聊场景」里给某个群开关「入群欢迎与验证」，/welcome 面板显示不变、新成员入群也不生效；
    反过来在 /welcome 里开，功能开关面板同样看不到。
  - **根因**：两个入口写的 \`scene_key\` 不是同一个 —— 面板写的是**成员级** \`group:<群ID>:user:<成员ID>\`，
    而 /welcome 写的是**群级** \`group:<群ID>\`。同一个开关，两个键，各自读各自的。
  - **现在**：功能开关的作用域链在群聊里展开成「**成员 → 本群 → 全局**」（新增 \`groupKeyOfScene\`），
    面板的群列表也**按群聚合**（原先一个群有几个成员就重复出现几条，点进去还是按成员设的）。
  - 成员级设置仍然优先于群级，历史数据不受影响；面板来源会显示「本群设置」。
  - 顺带修一处点进去就报错的路由 bug：\`callback.js\` 用 \`parseInt\` 解析场景 token，
    群级 token \`gc<群ID>\` 会被转成 \`NaN\`，点进群只能得到「参数无效」。

### 🧪 测试

- 测试 440 → **448 个**：新增 \`groupKeyOfScene\`、群级设置对整群生效、成员级优先于群级、
  群列表按群聚合、**走真实回调入口**打开群面板、面板与 /welcome 写同一个键、群级「恢复跟随全局」、
  群面板来源文案

## v3.9.0 · 2026-09-19

> 👋 补上群管理闭环（入群欢迎 + 人机验证），把 AI 用在「回复的那条消息」上，并修掉两处会被放大的隐患。

### ✨ 新增

- 👋 **入群欢迎与人机验证**（默认**关闭**，用群里 `/welcome` 开启）
  - 新成员入群自动欢迎，欢迎语支持 `{name}` / `{group}` 占位符，可在面板里引导式编辑
  - 可要求先**通过验证**：机器人先限制新成员发言，对方点「✅ 我已阅读群规」才放开
  - 超时（5 / 10 / 30 分钟可选）未验证 → 默认移出群聊（先封再解，允许以后重新加入），也可配置为仅解除限制
  - 收的是 `message.new_chat_members`（普通消息更新，**不需要**给 webhook 加 `allowed_updates`，老部署不用改任何配置）
  - 群主 / 管理员、以及机器人自己都不会被限制；机器人缺「封禁用户」权限时自动降级为「只欢迎」并说明
  - 功能开关新增 `defaultEnabled: false`：**会主动打扰群成员的功能默认不生效**，避免升级一次就往所有群发消息
- 📎 **AI 引用消息**：回复某条消息再 @机器人，就可以让它总结 / 翻译 / 解释那一条。引用内容作为「不可信素材」进提示词（明确要求不执行其中的指令），并且**不额外调用模型**（只拼上下文，不改变计费）
- 🔀 **知识库重排序（可选）**：设 `KB_RERANK_MODEL`（如 `@cf/baai/bge-reranker-base`）后，候选多于 `TOP_K` 时用交叉编码模型重排。**只改顺序不改门槛**，模型报错/返回异常自动回退原排序；默认关闭，因为要多花一次 Workers AI 调用

### 🐛 修复

- 🔴 **RAG 全量补读撞 D1 绑定参数上限（v3.8.0 自己引入的回归）**：`loadEmbeddings` 把最多 800 个 id 一次塞进 `WHERE id IN (?,?,…)`，而 **D1 单条语句最多 100 个绑定参数** —— 查询被拒后报错又被上层 catch 成「本次只用关键词」，于是「纯语义检索」在最需要它的场景下**一条都召回不到**。现在按 `KB.EMBED_ID_BATCH`（默认 80）分批。node:sqlite 不会复现这个上限，所以补了「参数个数守卫」测试
- 🛒 **`use_value` 可被「改价」绕过**：添加 / 编辑「背包用法」时都会拦「兑换积分 > 售价」，但**改价分支原先只看整数** —— 先配「售价 100、兑换 100」（合法）再把售价改成 1，就成了买 1 分兑 100 分的稳定套利。现在改价同样校验，并加数据迁移收敛历史上留下的超额配置
- 🔴 **21 点超时退款失败仍删记录**：退款失败（D1 抖动、用户记录缺失）时原先仍会无条件 `DELETE`，这笔本金就永远找不回来了。现在只删「退款成功 / 本来没本金」的行，失败的下个 tick 重试
- 🐛 **`processPendingDeletes` 的 N+1**：每行一条 `DELETE`，30 条/次 × 720 次/天 ≈ 2 万条多余语句。改成循环后一条 `IN` 收口

### ⚡ 性能（D1）

- **日级清理合并成一次 `env.DB.batch`**：`cleanupStaleData` 原先 13 条串行 `await`（13 个 D1 往返），现在一次批量执行
- **商城首页 / 背包页并行取数**：计数、积分、背包件数三条互不依赖的查询改用 `Promise.all`（原先 3~4 条串行）

### 🧰 工程

- 🧷 **`npm run check` 新增文档一致性校验**：`README.md` / `CHANGELOG.md` 声明的版本号必须等于 `package.json`，文档里写的测试用例数必须等于 `test/*.test.mjs` 的实际数量。这两处是历史上反复漂移的地方（v3.7.0 修过测试数，v3.8.0 之后 README 又停在 v3.2.2），现在由机器盯
- 修正文档漂移：README 版本号 `v3.2.2 → v3.9.0`、测试数 `365 → 440`，`docs/development.md` 同步

### 🧪 测试

- 测试 407 → **440 个**：新增 `welcome`（18）、`kb-rerank`（7），并补 `ai-path` 的引用消息用例（4）、`shop-hardening` 的改价绕过用例（3）、`schema` 的迁移收敛用例（1）

### 📌 说明

- 入群验证的**状态流转全部走带条件的原子 `UPDATE`**（`pending → passed` / `pending → expired`）：连点验证按钮、cron 与用户点击并发，都只有一次能改到行，不会重复解除限制或重复踢人
- `/welcome` **刻意不挂** `welcome` 功能开关：开关默认关闭，如果命令也被自己拦住，管理员就再也进不去把它打开
- 本轮审查还发现几条待办，已记录但**未在本次修改**：撤销处置 / 申诉批准缺原子占用、21 点「双倍」可绕过 `MAX_BET`、自定义群组标签权益不消耗、`cache.js` 定向失效的 key 少了作用域前缀

## v3.8.0 · 2026-09-19

> 🧰 把上一轮审查里「发现了但没修」的 7 件事全部落地。

### 🐛 修复

- 📡 **Telegram API 的退避被截断**：`MAX_BACKOFF_MS` 只有 8 秒，而 Telegram 的 `retry_after` 动辄 30 秒 —— `Math.min` 一截断就变成「等 8 秒再撞一次 429」，3 次尝试 16 秒耗尽**仍然失败**（纯白等）。现在听 Telegram 的（单次上限 30 秒），并加**总预算 45 秒**：超预算就干脆放弃，别把 webhook 拖到 Telegram 的 60 秒超时触发重推。
  - 这段逻辑此前**零测试**：现在 `parseRetryAfter` / `backoffMs` / `withinBackoffBudget` 都是可测的导出函数，`test/telegram-api.test.mjs` 覆盖了 5xx 重试、网络错误重试、业务错误不重试等路径。
- 🔑 **权限判断不一致（可见性 ≠ 能用性）**：
  - `/broadcast` 在注册表里声明了 `capability: broadcast`（admin 有），但 handler 里还硬判 `isMaster`（仅 owner）—— 菜单与 `/help` 给 admin 看了，点下去却是「权限不足」。现在权限只由注册表的 capability 决定。
  - `message.js` 里 4 处引导式输入（添加商品 / 知识库 / 群规）同样只认 owner：**admin 点了「添加文档」后回复的正文不会被消费，还会掉进 AI 对话扣 1 积分、正文永久丢失**。现在统一走 capability。
  - 封禁豁免也从「仅 owner」改成「任何机器人管理员」，兜住「先被封、后授权」的历史数据（正常途径本来就封不了管理员）。
  - 新增跨模块守卫：「注册表里每条管理指令的 capability，至少要有一个角色真的能用」—— 防止再出现「挂上去没人点得动」。
- 🛒 **商城三处**：
  - **限购原子化**：原先「先 SELECT 计数、再 INSERT」，并发双击会都读到未超限 → 两份订单 + 两次扣分 + 两次扣库存（只能靠事后重新计数取消多出来的那单，留下中间态噪音）。现在把限购条件写进 `INSERT ... SELECT ... WHERE`，只有一单能落库。
  - **部分使用不退全款**：订单买 2 件、只用掉 1 件时，原先会退**全款**却只回收剩下那件 —— 等于白送用掉的那件。现在「只要有一件被用过就整单不可退」（与 AGENTS.md 的约定一致）。
  - **兑换积分必须 ≤ 售价**：`use_value > price` 就是「买 1 分兑 100 分」的稳定套利。添加 / 编辑商品的引导流程都会拦下，免费商品也不允许配「换积分」用法。

### ⚡ 性能（RAG）

- 🧠 **问题向量缓存**：RAG 检索在回答**之前**，每次都要把问题送去 bge-m3；群里多人问同一句、或反复问同一个 FAQ 时这份调用完全重复。现在按「模型名 + 去空白小写的问题」在 isolate 内缓存 5 分钟（换模型自然失效；**失败不缓存**，否则模型恢复后也一直走降级）。
- 📚 **候选裁剪**：`embedding` 是 base64(1024 维 Float32) ≈ **5.4KB/块**，原先检索会把整个知识库（上限 400 块）连同向量一起读进内存 —— 满库时约 **2MB/条消息**，最终只用得上 4 条。现在先读轻量列做关键词排序，只给最有希望的 40 条补读向量；**关键词完全没命中时仍全量读取**（纯语义检索的能力不能丢）。

### 🧰 运维

- 💾 **备份补齐**：
  - 新增 `npm run backup:info`：查 D1 Time Travel 的当前 bookmark 与恢复命令（本机已核实可用）
  - `npm run backup` 默认**只保留最近 10 份**同类快照（`--keep` 可调）并打印文件大小，避免 `backups/` 无限膨胀
  - `docs/deployment.md` 重写备份章节：讲清 **SQL 快照 vs Time Travel 的分工**、各自的恢复命令，以及「Time Travel 恢复是整体回退」的警告

### 🧪 测试

- 测试 365 → **407 个**：新增 `telegram-api`（9）、`permission-consistency`（7）、`ai-path`（8）、`shop-hardening`（7）、`kb-embed-cache`（6）、`kb-candidate-trim`（5）

### 📌 说明

- 补 `ai-path` 时顺带确认了一个容易误读的设计：`daily_stats.count` **同时表示**「今日额度用量」和「今日消息量」，所以 `maxDaily = -1`（不限）时条数照样记录、只是不拦截 —— 不是 bug。
- 两个既有测试因为 `use_value ≤ price` 新校验而失败（售价 200 兑 300、售价 10 兑 500）——那些正是**白送分配置**，改的是测试数据而不是放宽校验。

## v3.7.0 · 2026-09-19

> 🛡️ 一次四维度代码审查（性能成本 / 并发正确性 / 安全权限 / 测试盲区）后的集中修复：堵掉一个**正在持续刷分**的赔率漏洞、一条**任意群管理员能全局封禁管理员**的越权路径，以及两处会重复执行的资金 / 执法动作。

### 🐛 修复

- 🔴 **老虎机与幸运转盘的返还率 > 1（可无限刷分）**：按代码倍率表实算是老虎机 `(1×50 + 5×10 + 90×2)/216 ≈ 1.296`、转盘 `0.30×0 + 0.25×0.5 + 0.20×1 + 0.15×2 + 0.08×5 + 0.015×10 + 0.005×50 = 1.425` —— **都是正期望**，配合「ALL IN」+ 无限重复就是稳定刷分。生产数据也印证了：幸运转盘 10 次让玩家净赚 **4650** 分。
  - 老虎机改成 8 个图标（`(1×50 + 7×15 + 168×2)/512 ≈ 0.959`）；转盘档位重排为 `33%×0 / 25%×0.5 / 20%×1 / 14%×2 / 5%×3 / 2.5%×5 / 0.5%×15 ≈ 0.955`
  - 倍率表抽成可导出常量，`test/game-odds.test.mjs` 加**返还率守卫**（与抽奖的 `expectedPrize() < PAID_COST`、轮盘的 `36/37` 同一思路）；顺带补齐四个游戏 `play()` 的零覆盖 —— 原先骰子 / 抛硬币 / 老虎机 / 转盘的结算函数**一个测试都没有**，这个漏洞正是因此藏了很久
- 🔴 **任意群管理员可以把机器人管理员全局封禁**：「不可处置 / 不可封禁」的三处兜底（`executePunishment`、`setUserBlocked`、`banUserById`）原先**只认 owner**。攻击路径：随便谁建个群把机器人拉进去（他天然是本群管理员），`/ban <某 admin 的 id> 广告` → 确认 → 该 admin 被写进全局 `users.blocked`，之后私聊与按钮全被拦、**连 `/unban` 都发不出去**，只能由 owner 手动解。
  - 新增 `isBotAdmin()`（走 `getAdminRole`，覆盖 owner / admin / moderator），三处统一改用它；`test/admin-protection.test.mjs` 含这条攻击路径的端到端复现
- 🔴 **群规「确认执行」可重复执法**：确认流程是「先判断 pending、再调 Telegram」，双击确认卡片（或 Telegram 重推回调）会让两个请求**各执行一次** —— 目标被重复封禁，公告 / 私聊 / 审计各发两遍。新增 `claimPunishment()` 原子占用（`pending → executing`，`meta.changes === 1` 才继续），并给「执行中断卡在 executing」加了 30 分钟兜底（标记 failed，保持「失败不可重试」的语义，不冒重复执法的风险）
- 🟡 **单局下注没有上限**：以前只受余额约束，「ALL IN」一次手抖就能清零（生产里出现过一笔 **110918 分**的全押）。新增 `MAX_BET = 1000`，自定义下注面板收敛到 `[1, min(余额, 上限)]`，六个游戏的 `play/start` 也都会挡下超限下注

### ⚡ 性能（D1）

- **cron 拆分「及时型」与「日级」**：原先每 2 分钟的 tick 都会跑一遍日级清理（15 条 DELETE）与**只给日报用的 7 条统计**（还都是全表扫描），720 次/天纯属白做。现在及时型（长延时删除 / 超时牌局退款 / webhook 自愈）每次跑，日级（清理 / 统计 / 日报推送）只在 `0 16 * * *` 跑。
  - `reindexKnowledge` **刻意留在每次 tick**：换向量模型后靠它分批补建（每次 20 块），放进日报会让 400 块补 20 天
- **补 5 个索引**（Schema v20）：`daily_stats(date_str)`、`daily_checkin(date_str)`、`users(blocked)`、`redeem_logs(created_at)`、`group_punishments(status, until_at)`
  - 其中 `users(blocked)` 必须写在 `MIGRATIONS` 里而不是 `SCHEMA_SQL`：`blocked` 是迁移后加的列，老库上先建索引会 `no such column` 并让整个建表 batch 失败（`test/schema.test.mjs` 当场抓到了这一点）

### 🧹 清理

- 删掉 **13 个零引用导出**：`sendUserManageMenu`、`unbanByUserId`、`SHOP_MSG`、`buildToolResultText`、`cacheClearAll`、`loadScopedSetting`、`setScopedSetting`、`clearScopedSetting`、`getActivePunishment`、`getTodayCount`、`resetTodayCount`、`toggleBlockBySceneRow`、`downloadFileText`
- **修正文档漂移**：测试用例数（260 → 365）、README 与 configuration 里的 cron 只写了 1 条（实际 2 条）、架构文档写「4 个游戏」（实际 6 个）、`configuration.md` 缺 `WEBHOOK_URL`
- `scripts/check.mjs` 的注释声称「校验 DB Schema 里是否出现了未声明的表」，实际没实现 —— 改成如实描述，别留「已经查过了」的错觉

### 🧪 测试

- 测试 334 → **365 个**：新增 `game-odds`（16）、`admin-protection`（6）、`guard-atomic`（5）、`cron-split`（4）

### 📌 说明

- 本次是**审查驱动**的修复：性能 / 并发 / 安全 / 测试盲区四个维度分别审，每条都带 `文件:行号` 证据 —— 索引有没有走到用 `EXPLAIN QUERY PLAN` 核过，路径查询数用 D1 替身实测（私聊一条走 AI 的消息 = **15 条语句**）。
- 审查确认**没问题**的方向也一并记下：无 SQL 注入（拼进 SQL 的都是常量表名或白名单列名）、密钥从未进过 git 历史、webhook 校验无绕过、主路径索引齐全（全部 SEARCH）、兑换码 / 签到 / 抽奖免费次数 / 订单取消退款的原子性都对、没有 IDOR。
- **还没做的**（按优先级留作后续）：RAG 候选全量读取（满库约 2MB/条消息，随知识库线性恶化）、问题向量化无缓存、串行 `await`（商城首页 4 条、cron 15 条本可 batch）、`telegram/api.js` 退避重试零测试且 `retry_after` 被截断到 8 秒、AI 主链路「扣分 / 占额 / 回滚」零测试、商城限购与背包退款的竞态、背包 `use_value` 无上限校验。

## v3.6.1 · 2026-09-19

> 🐛 修一个**资金安全**漏洞（v3.5.0 引入）：21 点连点「停牌」可以重复领奖。由一次四维度代码审查发现。

### 🐛 修复

- 🔒 **连点「停牌」重复领奖**：结算顺序本来是「算赔付 → 发奖 → 写流水 → 要 AI 台词（**最长 8 秒**）→ 删牌局」，而删牌局是裸 `DELETE`、不校验影响行数。Telegram 的回调会被连点、也会重推；两个请求同时读到同一局就各发一次奖 —— 赢 100 分的局能收两次。那 8 秒的模型调用把窗口放得极大。
  - 现在结算前先**原子抢结算权**：`DELETE ... WHERE status = 'playing'`，只有 `meta.changes === 1` 的那次请求有权发奖，另一次明确回「这局已经结算了」
- 🔒 **并发双击「确认下注」可能扣了分却没有牌局**：原来的 `saveSession` 是 upsert，两个请求都读到「没有进行中的牌局」时都能扣分并写入。而**超时退款扫的是牌局表**，记录没建成的那笔分就永远找不回来。
  - 现在建局用 `INSERT ... ON CONFLICT DO NOTHING`，插入失败的那一份**立刻退还本金**并提示
- 🔒 **慢请求会把已结算的牌局「写活」**：`saveSession` 的 upsert 若在原子删除之后才落库，会重新插入一条 `playing` 记录，用户就能拿同一局反复结算
  - 现在 `hit` 的保存改成只更新 `status='playing'` 的行，更新不到就提示这局已结束
- 🐛 同时调整：Blackjack（首两张 21 点）直接结算的分支改成**先落库再结算** —— 原子抢占需要会话存在才能生效，否则「中了 Blackjack 反而拿不到钱」

### 🧪 测试

- 测试 331 → **334 个**：新增三条并发用例
  - 连点「停牌」两次只结算一次，且流水只该有一条
  - 会话被另一个请求抢走（原子删除）后不再发奖，并明确提示
  - 并发双击下注时第二份本金必须退回 —— 用「**已过期但主键仍在**」的会话精确构造「两个请求同时读到没有牌局」的那一瞬间

### 📌 说明

- 这类「先 SELECT 判断、再写」的竞态在别处也可能存在，已把结论记进 `AGENTS.md` 的多轮牌局约定里：**涉及发奖的状态流转必须由一条带条件的原子语句做唯一凭据**。

## v3.6.0 · 2026-09-19

> 🔴⚫ 游戏中心第六个游戏：轮盘赌。也是第一个**数学模型自带抽水**的游戏 —— 不用像老虎机那样自己调权重。

### ✨ 新增

- 🔴⚫ **轮盘赌（欧洲轮盘，单零 37 格）**：入口在游戏大厅（`/game` → 「🔴⚫ 轮盘赌」）
  - **9 种下注**：红 / 黑、单 / 双、小(1-18) / 大(19-36) 赔 **1:1**；1-12 / 13-24 / 25-36 赔 **2:1**
  - **庄家优势来自 🟢 0 通杀所有外围注**：37 格里只有 36 格算外围，所以四种下注的返还率都是 `36/37 ≈ 97.3%`
  - 卡片显示**近 10 期开奖**（全局共享、纯展示），能看「走势」
  - 押中后可「再押同样的」或「换个押法」，金额沿用上一把
  - 开奖是**一次下注一次开奖**，不需要牌局状态表 —— 与骰子 / 抛硬币同级，零 schema 变更

### 🛡️ 加了一条「返还率守卫」

- `test/roulette.test.mjs` 里断言**每种下注的返还率都必须是 36/37 且 < 1**。这是给未来的改动上锁：赔率表一旦被改错（变成 ≥ 1 的正期望），游戏就成了刷分渠道，会直接把积分体系（商城 / 兑换码）冲垮。和抽奖那边的 `expectedPrize() < PAID_COST` 是同一个思路。

### 🧪 测试

- 测试 313 → **331 个**：新增 `test/roulette.test.mjs`（18 个用例：红黑各 18 个且 0 是绿、单双 / 大小 / 三打判定、0 通杀、返还率守卫、1:1 与 2:1 的结算金额、开奖扣分与流水、0 通杀在真实流程里、积分不足与未知下注被挡、历史 10 期与排序、历史展示、键盘排版、选注 / 结果键盘、二级界面、大厅集成）

### 📌 说明

- **没做单号 35:1**：37 个数字放不进 Telegram 键盘（项目约定整个菜单 ≤ 8 行、每行 ≤ 2 个按钮），而且 1/37 的中奖率下「点三次选号还没中」的体验很差。想要的话可以再加「分页选号」或「引导式输入选号」，需要就说一声。
- 这个游戏**没有接 AI**：轮盘是纯概率游戏，接模型只会白烧额度（而且每次开奖一次调用，频率太高）。AI 更适合「AI 猜词 20 问」那种非它不可的玩法。

## v3.5.0 · 2026-09-17

> 🃏 游戏中心多了第五个游戏，也是第一个**多轮牌局**：21 点，庄家由 AI 充当

### ✨ 新增

- 🃏 **21 点（Blackjack）**：入口在游戏大厅（`/game` → 「🃏 21 点（AI 庄家）」），支持要牌 / 停牌 / 双倍下注
  - **规则**：单副 52 张、每局重新洗牌；A 自动取不爆的最大值；首两张 21 点 = Blackjack 赔 **3:2**（双方都 Blackjack 平局退本）；庄家**不足 17 点必须继续要牌**、到 17 停牌（软 17 也停）；平局退本金
  - **双倍下注**：只在首两张时可用 —— 本金翻倍、只再补一张牌后自动停牌
  - **AI 只负责说话**：每一步判定都走固定规则（公平、可验证、不拖慢节奏），模型只在**结算时**说一句台词（挑衅 / 打趣 / 认栽）。**每局 1 次调用**，调用失败或 8 秒超时自动回退内置台词，绝不卡住结算
  - **多轮状态**：新表 `blackjack_sessions` 存牌局（含**剩余牌堆**），一个会话一人一局；从大厅再进来会接着打，不会重复扣分
  - **不会吞分**：开局先扣本金，结算按输赢返还；牌局 **30 分钟**没动作由定时任务作废并**退还本金**；双倍用原子条件（`WHERE doubled = 0`）拦住重复点击，重复的那次会把多扣的份额退回去
  - 庄家的第二张牌在结算前显示为 🎴，不会提前泄漏暗牌

### 🔧 优化

- 💰 `refundPoint()` 现在返回退款后的余额（原来是 `undefined`），方便调用方判断退款是否真的成功；现有调用方不受影响
- 🧹 定时任务清理结果里新增 `blackjackSessions` / `blackjackRefunded` / `blackjackFailed` 三个计数，日志里能直接看到退了几笔

### 🧪 测试

- 测试 283 → **313 个**：新增 `test/blackjack.test.mjs`（A 的软硬点数、Blackjack 与爆牌判定、庄家要牌规则、六种结算赔付含 3:2 与奇数注取整、洗牌不重复、开局扣分与落库、积分不足拒绝、已有牌局不重复扣分、要牌 / 爆牌 / 停牌 / 庄家补牌、双倍与重复双倍、结算后旧按钮失效、暗牌不泄漏、超时退款与未到期不清、AI 台词回退与清洗与转义、大厅排版与按钮可见性）

### 📌 说明

- **为什么不让模型决定要不要牌**：它可能 20 点还要牌，用户只会觉得机器人在乱来或者作弊；而且每要一张牌就多一次调用（2~5 秒），牌局会被拖垮。想让 AI 更有存在感，可以调 `BLACKJACK` 相关的内置台词，或换更强的模型。
- 21 点是**单副牌、不做 split / 保险**的简化版。想要分牌或保险得把状态机与下注再扩一层，需要就说一声。

## v3.4.0 · 2026-09-17

> 🩺 修一种**没有任何日志**的无声故障：Telegram 侧的 webhook 地址会被清空，此时 Worker、Token、日志全都正常，只是再也收不到消息。现在定时任务会盯着它，空了就自动补回并私聊告知。

### 🐛 修复

- 🕳️ **「换了 Bot Token 之后机器人再也叫不醒」**：在 BotFather 里撤销 / 更换 token 后，Telegram 侧的 webhook 地址会**变成空串**，所有更新因此无处投递。
  - 实测时间线：22:53 还能正常投递更新；用户撤销并换新 token 后，23:19 复查地址已是空的（`wrangler deploy` 不碰 webhook，所以**不是部署的问题**）
  - 排查成本极高：`getMe` 正常、Worker 根地址正常、`wrangler tail` 一条错都没有 —— 只能靠主动查 `getWebhookInfo`
  - 现在由巡检发现并自动恢复（见下）

### ✨ 新增

- 🔗 **Webhook 自愈巡检**（`src/services/webhook.js`，挂在那条每 2 分钟的 cron 上）
  - 地址为空 → 用「期望地址」自动 `setWebhook` 补回，并私聊管理员一条告警
  - 有投递错误（`last_error_message`）→ 私聊告警，**内容变了才提醒**，不会每轮刷屏；投递恢复后标记自动清空
  - 地址非空但与期望值不同 → **保持不动**（可能是你有意配的自定义域名 / 反代，自愈不该覆盖它）
  - isolate 内 10 分钟最多查一次，避免 2 分钟的 cron 把接口刷爆
  - 期望地址来源：`WEBHOOK_URL` 优先；没填则用「上一次**通过 secret 校验**的请求地址」——只有 Telegram 知道 `WEBHOOK_SECRET`，所以伪造请求改不掉它
- 🛡️ **来源校验写进模板**：`wrangler.toml` 补上 `WEBHOOK_SECRET`（Telegram 回传的校验串）与 `WEBHOOK_URL`（自愈目标）的说明；两个都留空 = 完全关掉自愈
- 🧰 `src/telegram/api.js` 补 `getWebhookInfo` / `setWebhook` 两个封装（走统一的 429/5xx 退避重试）

### 🧪 测试

- 测试 273 → **283 个**：新增 `test/webhook.test.mjs`（期望地址优先级、伪造请求不登记、只记 origin 丢掉 path、恢复时带 `secret_token`、恢复失败如实返回、地址不同不覆盖、投递错误只告警一次、未知地址不查接口、isolate 节流、入口 401 与地址登记）

### 📌 说明

- **换 Token 的正确顺序**：先 `setWebhook`（带新的 `secret_token`）→ 再 `npm run deploy:prod`。反过来的话，新 Worker 会因为校验不过而把更新全拒掉（401），机器人看起来还是死的。
- 自愈**只在地址为空时动手**；想彻底禁用，保持 `WEBHOOK_SECRET` 与 `WEBHOOK_URL` 都为空即可。
- 本仓库生产环境已启用：`WEBHOOK_SECRET` 为随机串，`WEBHOOK_URL` 指向 `https://tgbot.zoolp.workers.dev`。

## v3.3.0 · 2026-09-13

> ✨ 商城补齐两件事：买到的虚拟物品进「🎒 我的背包」由用户自己取用；已完成订单支持退款（没使用过的物品自动收回）

### ✨ 新增

- 🎒 **我的背包**：商品发放方式多了一种 `bag` —— 下单即完成、不打扰管理员，物品直接进背包，用户自己挑时间用。
  - 入口：`/bag`（别名 `/backpack` `/items`）与「🛒 积分商城 → 🎒 我的背包」，仅私聊
  - 未使用的物品排在前面并分页（每页 5 件），每件未使用的物品配一个「✅ 使用」按钮
  - **用法配在商品上**（`shop_items.use_type` / `use_value`），并在**下单时快照**进背包记录 —— 之后改商品配置，不影响已经买到的物品
    - `none`（默认）仅核销：点「使用」后标记已用，私聊通知管理员去交付 / 核销
    - `points` 使用后兑换成积分：按快照的积分数原子加分 + 写 `points_log`
- ↩️ **已完成订单退款**：订单状态多了一个 `refunded`
  - 用户自助：**背包物品还没使用**的订单，在「📜 我的订单」点「↩️ 退款」即可 —— 退积分 + 回滚库存 + 收回物品
  - 管理员：订单详情里对 `done` 订单多一个「↩️ 退款（收回未使用物品）」按钮
  - 物品**已经使用过**就不能退（用户与管理员都不行）——东西已经交付了
- 🛠️ **商品配置跟上**：`/shop_add` 第 7 步选发放方式（人工 / 群标签 / 进背包），选「进背包」继续问用法与积分数；`/shop_edit` 新增「🚚 改发放方式」「🎒 改背包用法」两个字段

### 🐛 修复

- 🕳️ **限购漏洞**：并发超出限购的订单原来只收回 `pending` 状态的那一单，而自动发放的订单（下单即 `done`）会出现「东西已经发了、积分也退了」。现在 `pending` 与 `done` 一起收回，进背包的还会把物品标记为已退回。

### 🔧 优化

- 🔁 限购统计不再把 `refunded` 订单算作名额（和 `cancelled` 一样），退过款还能再买
- 🎒 商城首页顶部显示「我的背包：N 件未使用」；商品详情页直接写明发放方式与用法
- 🧪 测试 260 → **273 个**：新增 `test/bag.test.mjs`（Schema 迁移、购买入库、使用换积分 / 仅核销、重复使用、重复退款、已使用不可退、管理员退款、退款后重新购买、菜单排版、添加 / 编辑引导流程）

### 📌 说明

- 退款是**自助即时到账**，不是「申请 → 等管理员审批」：只有「物品还在背包里没动」的订单能自助退，物品能原样收回、库存能回滚，不涉及已经交付出去的服务。
- 想在商城卖「红包 / 兑换券 / 道具」这类东西：`/shop_add` 里发放方式选 3（进背包）、用法选 2（换积分）就行。

## v3.2.2 · 2026-09-13

> 🐛 群主买「自定义群组标签」给自己用，填完标签才被 Telegram 拒：`Bad Request: CHAT_CREATOR_REQUIRED`

### 🐛 修复

- 🚫 **成员标签只对普通成员生效**：`setChatMemberTag` 的文档原文就是「a regular member」。**群主和管理员都拿不到成员标签**——群主的名字归「管理员头衔」管，而 `setChatAdministratorCustomTitle` 只能给机器人自己提升的管理员设置，机器人改不了群主。
- ✅ **选群时就提前拦下**：新增 `checkTagTarget()`，用户选群时先查一次身份——群主 / 管理员 / 已退群直接说明原因并让他换群，**不会再出现「填完标签才失败」**；填标签那一步再查一次，兜住身份变化。
- 💬 **错误有中文解释了**：`CHAT_CREATOR_REQUIRED` 不再原样丢给用户，翻译成「成员标签只对普通成员生效，群主和管理员都不行」。
- 🧪 测试 256 → **260 个**：覆盖群主 / 管理员 / 已退群三种提前拦下、进入填标签后才变身份、`CHAT_CREATOR_REQUIRED` 的可读解释。

### 📌 说明

- 这个商品目前是「**买家给自己**设置标签」，所以只对「在某个群里是普通成员」的人有意义。如果你想让它对群主也有用，可以改成「买家选群 → 再选该群里的某个成员来设置」（成员列表只能取机器人见过的群成员，Telegram 不允许机器人枚举群成员），需要就说一声。

## v3.2.1 · 2026-09-13

> 🐛 `/addpoints` 只认「场景行 ID」这一个写法，写用户 ID / `user:xxx` / `@用户名` 都报「参数无效」，而「参数无效」本身也没说清楚该写什么

### 🐛 修复

- 🎯 **`/addpoints` 目标写法放宽**：现在这些都能命中——

  | 写法 | 说明 |
  |------|------|
  | `/addpoints 12 100` | 场景行 ID（用户详情里的 `#12`，老用法不变） |
  | `/addpoints 8802544525 100` | Telegram 用户 ID |
  | `/addpoints user:8802544525 100` | 用户键（面板里显示的那种） |
  | `/addpoints @someone 100` | 用户名（对方得先和机器人说过话） |

  - 短数字优先当场景行 ID，长数字（≥5 位）找不到场景时才当用户 ID，老用法不会被打乱
  - **群 ID（`-100...`）会明确说明**「积分是按用户算的」，并列出该群前几个成员场景行 ID，而不是一句「未找到场景」；不会误给整群加分
- 💬 **报错能照抄**：目标写错时直接给出可用写法示例；数量不是整数会单独提示
- 📊 **回复说清结果**：显示 `<目标> 的全局积分：N（本次 +X）`；请求 9999999 这种超过上限的，会显示实际变动并提示「已按上限 0 ~ 1000000 夹断」
- 🧪 测试 252 → **256 个**：覆盖四种目标写法、群 ID 引导、夹断说明、格式提示

## v3.2.0 · 2026-09-13

> ✨ 商城多了一种商品：**买完不用等管理员发货**，机器人自己把东西交付给用户。第一件是「🏷️ 自定义群组标签」

### ✨ 新增

- 🚚 **商品发放方式**：`shop_items.delivery` 分出两条路径——
  - `manual`（默认）：和以前一样，下单是「⏳ 待处理」，通知管理员发货；
  - `group_tag`：**下单直接完成**，不通知管理员，立刻进入机器人自己的发放流程。
- 🏷️ **自定义群组标签（500 积分，内置商品）**：购买后选一个「机器人所在的群」，再发送想要的标签，机器人调用 Telegram 的 `setChatMemberTag` 直接设置成**你在那个群的个人标签**。
  - 群列表按「你也在的群优先」排序，一页 10 个；机器人已经退出的群会自动隐藏
  - 标签 1~16 字、不支持 emoji（Telegram 硬限制，服务端先校验再请求）；回复 `-` 可以清除
  - 需要机器人在目标群是管理员并有**「管理标签」**权限；缺权限时面板会直接说清楚，并让你换一个群
  - 会话 30 分钟有效；过期/放弃后可在「📜 我的订单」点「🏷️ 设置标签」重新进入——**用户已经付过钱，流程不能变成死路**
  - 用户自助设置，不打扰管理员；群名与权限检查结果缓存在新表 `bot_chats`
- 🗄️ **新表**：`bot_chats`（群名 / 权限缓存）、`group_tag_sessions`（引导流程，30 分钟 + 定时清理）、`user_group_tags`（当前标签 + 审计）
- 🧪 测试 238 → **252 个**：新增 `test/group-tags.test.mjs`（标签校验、迁移与内置商品、购买即完成、选群与设置、权限/内容兜底、放弃与重新进入、定时清理），并补了选群面板的排版用例

## v3.1.3 · 2026-09-13

> 🔧 多管理员在 v3.0.0 就上了，但「谁能看到哪些指令」还停在只有一个管理员的写法：菜单只挂给 `MY_TELEGRAM_ID`，`/help` 干脆没把角色传下去

### 🔧 优化

- ⌨️ **输入框菜单按身份挂载**：除「所有私聊 / 所有群聊」外，现在还会
  - 给👑拥有者私聊挂全部管理指令；
  - 给每个 🛡️管理员 / ⚔️执法员私聊挂**他有权用的那一份**（执法员不会看到兑换码、群发、用户管理）；
  - 给每个已知群挂 `chat_administrators` 作用域，本群管理员在群里输入 `/` 也能看到 `/ban`、`/mute`、`/kick` 等（此前群里菜单被写死成不含管理指令）。
- 🧹 **失效作用域会被清空**：移除管理员 / 改角色后，`setMyCommands([], scope)` 把他那份清掉，不会在输入框里留一串点不动的命令。哈希里带上挂载目标，名单一变就重新同步；授权 / 改角色 / 移除后还会立即刷新一次。
- 📖 **`/help` 接上角色**：`cmdHelp` 以前只用 `isMaster` 推断角色，除拥有者外的人全被当成普通用户——管理员/执法员点开 `/help` 只看到「仅管理员可用」。现在按 `capability` 裁，本群管理员（没有机器人角色）也会看到自己那几条执法指令。
- 📐 **可见性 = 能用性**：菜单与 `/help` 共用同一条规则（`capability` + `groupAdmin`），以后加管理指令只要带 `capability` 就自动对齐；见 AGENTS.md。
- 🧪 测试 235 → **238 个**：新增菜单按角色裁剪、多作用域挂载与「移除后清空」，以及 `/help` 对执法员 / 本群管理员 / 普通成员三种身份的用例。

### 📌 说明

- Telegram 的 `chat_administrators` 作用域按群挂，所以给群挂菜单时要先知道群 ID。目前取「机器人见过的群」（`user_scenes` 里的 group / supergroup），单次最多 20 个群、20 个管理员；超出的部分会在日志里提示，普通用户菜单不受影响。

## v3.1.2 · 2026-09-13

> 🐛 三个「明明配好了却不对」的问题：预警词对管理员不生效、每日概况一直弹、面板上的时间比北京时间早 8 小时

### 🐛 修复

- 🔔 **群规对所有人一视同仁**：`handleKeywordAlert` 原来会跳过机器人管理员与本群管理员，导致「自己发预警词机器人没反应」。现在**谁发言都扫**，owner / 机器人管理员 / 群管理员命中关键词同样私聊提醒。
  - 会把人锁在后台外面的动作仍然拦着：卡片照样发，但目标是机器人管理员时卡片上会写明「确认按钮会被安全策略拦下」，`executePunishment` / `requestPunishmentFromCommand` 的兜底不动（见「安全底线」）。
- ⏰ **每日概况不再每 2 分钟弹一次**：`runScheduledTasks` 以前每次 cron 都推概况，而 `*/2 * * * *` 那条兜底 cron 是给「长延时自动删除」用的 —— 于是概况一直弹。现在只有**每天一次**的 cron（`0 16 * * *`）负责推送，并额外用 `daily.last_summary_date` 标记兜底，**同一天只推一条**；兜底 cron 只做清理与删除。
- 🕒 **时间统一按北京时间（UTC+8）展示**：库里仍然存 UTC（比较、去重、到期判定都依赖它），但所有**给人看**的时间都过 `formatAppTime()` 换算到 `APP_TIMEZONE`：处置记录 / 群规历史 / 处置到期公告（原来硬编码 `UTC`）、订单列表与详情 / 下单通知、积分流水、操作日志、管理员授权时间、知识库文档时间、群发确认时间。
- 🧪 测试 228 → **235 个**：新增 `test/time.test.mjs`（时区换算与兜底）、日报推送去重用例、以及「owner 与群管理员发言同样预警」的回归用例。

## v3.1.1 · 2026-09-13

> 🐛 操作日志里一堆动作显示原始代号（`autodelete_set`、`kb_doc_scope`、`guard_default_mute`…），现在都有中文标签了

### 🐛 修复

- 📋 **操作日志中文标签补全**：v3.0 / v3.1 新增的动作当时只写了代码没补标签，面板上直接显示 `autodelete_cap` 这样的裸代号。现在补齐：

  | 分组 | 补上的标签 |
  |------|-----------|
  | 👑 管理员与权限 | 添加管理员 / 修改管理员角色 / 移除管理员 |
  | 🗑️ 消息自动删除 | 设置消息删除时长 / 恢复默认删除时长 / 设置全局兜底删除 |
  | 📚 知识库 | 调整文档生效范围 |
  | 🛡️ 群规执法 | 开关执法、默认处置、默认禁言时长、撤销处置、回滚群规版本、开关预警、修改预警关键词、关键词预警、申诉通过 / 驳回申诉 |
  | ⚠️ 历史 | 引导步骤发送失败（每日任务已下线，保留可读性） |

- 🔍 **筛选项补齐**：新增「👑 管理员」「🗑️ 自动删除」两个分类（原来这些动作会落在「系统」里）；「系统」的排除条件同步更新
- 🏷️ **未知动作兜底**：以后新增动作忘了加标签时，显示 `⚙️ 原始代号` 而不是裸代号，一眼能看出是「忘加标签」而不是「日志坏了」
- 🧪 新增用例锁定：v3.x 的所有动作都必须有中文标签，未知动作走兜底；筛选菜单含 9 个分类（仍 ≤ 8 行）

## v3.1.0 · 2026-09-13

> ✨ 消息自动删除多了个**最高优先级兜底**：分类型设置照常跑，但到了兜底时间，机器人所有消息一律删掉

### ✨ 新增功能

- 🌍 **全局兜底（最长保留时间）**（管理控制台 → 🗑️ 自动删除 → 第一行）
  - 可选 **未设置 / 30 分钟 / 1 小时**
  - 语义是**上限**而不是替换：`实际删除时间 = min(该类型自己的时长, 兜底)`；本来设成「不删除」的消息（AI 回复、确认卡片、公告）也会在兜底时间被删
  - 面板上会写明当前兜底值与说明；只对**设置之后**发出的消息生效（不追溯删除历史消息）
- ⏱️ **长延时改用定时任务执行**：Worker 的 `waitUntil` 撑不住几十分钟，所以 ≥1 分钟的延时不再 sleep，而是登记到新表 `pending_deletes`，由 **每 2 分钟**的 cron 扫描删除（误差 ±2 分钟；超过 48 小时的记录自动清理，因为 Telegram 不允许删更早的消息）

### 🗄️ 数据库

- 新增 `pending_deletes`（待删除的机器人消息），`SCHEMA_VERSION` 15 → **16**

### ⚙️ 部署注意

- `wrangler.toml` / `wrangler.production.toml` 的 `[triggers] crons` 增加 `"*/2 * * * *"`：

  ```toml
  crons = ["0 16 * * *", "*/2 * * * *"]
  ```

  部署时会一并更新触发器；不带这个 cron 的话，30/60 分钟的兜底不会执行

### 🧪 测试

- `test/auto-delete.test.mjs` 扩到 15 个用例：兜底取值（min 语义、场景更长时以兜底为准、取消兜底）、长延时登记而非 sleep、定时任务删除到点消息并清理过期记录、面板设置 30 分钟 / 1 小时 / 取消

## v3.0.1 · 2026-09-13

> 🐛 修「AI 查数据不好使」：问积分却回「请先使用 get_my_points 指令」——工具名被模型当成指令讲出来了

### 🐛 修复

- 🤖 **AI 查数据不灵（v3.0.0 的协议设计问题）**：v3.0.0 让模型自己决定「要不要调用工具」，做法是把工具名与 JSON 约定写进系统提示词。实测 llama 系模型不按约定输出 JSON，反而把工具名当成指令 / 资料讲给用户听：

  ```
  用户：@bot 我有多少分
  旧版：管理员，我需要您先使用 "get_my_points" 指令来查询您的积分余额… — 摘自《get_my_points》
  ```

  现在改为**确定性预取**：

  1. `detectToolIntent` 用关键词识别常见问法（「我有多少分」「签到几天了」「看看排行榜」「群规是什么」）
  2. 直接执行查询，把结果作为**实时数据**拼进上下文——**提示词里完全不出现工具名**
  3. 模型只负责把数据讲清楚，**一次模型调用**搞定（比旧方案还省一次调用）
  4. 注入文本里明确要求「不要提工具 / 不要标注《》来源」，避免再出现「摘自《…》」

- 🧹 顺带删掉了「输出 JSON → 再问一轮」的旧路径；`parseToolCall` 仍保留，留给将来换支持原生 tool calling 的模型时复用

### 🧪 测试

- `test/ai-tools.test.mjs` 扩到 9 个用例：意图识别（含闲聊不误触发、私聊不查群规）、实时数据拼接（断言**不含工具名**）、端到端只调一次模型、群规预取、开关关闭不预取

## v3.0.0 · 2026-09-13

> ❗ **一个大版本**：机器人从「单管理员 + 只认指令」升级成「多管理员分工 + AI 会查数据」 —— 五条主线一起落地

### ❗ 破坏性变更

- 👑 **权限模型换了**：不再「只有 `MY_TELEGRAM_ID` 能进后台」，而是 **角色 → 能力**

  | 角色 | 能做什么 |
  |------|---------|
  | 👑 拥有者（`MY_TELEGRAM_ID`） | 全部，含管理员与权限管理 |
  | 🛡️ 管理员（后台授权） | 用户管理、商城、知识库、群规配置、功能开关、自动删除、兑换码、群发、日志统计、执法 |
  | ⚔️ 执法员（后台授权） | 只能执法：封禁 / 踢出 / 禁言 / 群规查看 / 处理举报与申诉 |

  - 默认行为不变：`bot_admins` 为空时，只有拥有者能进后台（和以前完全一致）
  - 新增 `/admins` 与「👑 管理员与权限」面板：**引导式**加人（输入 ID → 选角色 → 备注）、改角色、移除（二次确认）
  - 拥有者不可被移除、不可被封禁；每次变更都进审计日志

### ✨ 新增功能

- 🧩 **统一配置模型**：`services/config.js` 用一条**作用域链**解析配置（场景 → 群 → 全局），并返回「生效值 + 来源」
  - 功能开关与消息自动删除都改成走它，面板上直接显示「本场景设置 / 本群设置 / 全局默认」
  - 不再出现「我改了到底生不生效」的困惑
- 🛠️ **AI 工具调用**：AI 现在能查真实数据了

  | 工具 | 作用 |
  |------|------|
  | `get_my_points` | 查自己的积分 |
  | `get_my_checkin` | 查签到天数 |
  | `get_rank` | 积分排行榜前 5 |
  | `get_group_rules` | 本群群规 |
  | `search_knowledge` | 知识库检索 |

  - 模型需要数据时输出一行 JSON，机器人执行后把结果喂回去再要最终回答；**只允许一轮**，避免费用失控
  - **只读**：写操作（封禁 / 发积分 / 改配置）不在工具列表里，仍必须走显式指令 + 确认卡片
- 💸 **积分转账**：`/transfer`，支持用户 ID / @用户名 / 回复消息三种指认方式；原子扣分、失败自动退回、双方各记流水
- 🎁 **每日抽奖**：`/lottery`，每天 1 次免费 + 可按次花积分抽；奖池权重可配，**期望值低于单次成本**，不会被当成刷分入口
- ⚡ **工程底座**：
  - isolate 级 TTL 缓存（按数据库实例隔离，写路径主动失效）——群消息不再每条都查 D1
  - 异常自动私聊管理员（同类错误 5 分钟去重），半夜炸了也不至于没人知道

### 🗄️ 数据库

- 新增 `bot_admins`（管理员与角色）、`admin_manage_sessions`（加人引导状态）、`lottery_draws`（抽奖记录，含「每天免费一次」的部分唯一索引）
- `SCHEMA_VERSION` 13 → **15**

### ⚙️ 功能开关

- 新增两个开关：`transfer`（积分转账）、`lottery`（每日抽奖）、`ai_tools`（AI 工具调用）——都支持全局 / 场景两级

### 🧪 测试

- 测试 193 → **220 个**：新增 `test/admins.test.mjs`（角色矩阵、命令与回调拦截、引导式加人、菜单 / 帮助裁剪）、`test/points-play.test.mjs`（转账与抽奖的全部分支）、`test/ai-tools.test.mjs`（工具解析、只读执行、一轮调用闭环、开关关闭）

## v2.9.0 · 2026-09-12

> ❗ **下线「每日任务」** —— 管理面板、`/tasks` 指令、全勤奖、相关数据表与统计字段一并移除

### ❗ 破坏性变更

| 项 | 变化 |
|----|------|
| `/tasks`（别名 `/daily`） | 指令移除，输入框菜单项随之消失 |
| 管理控制台 → ✅ 每日任务 | 面板与入口移除 |
| 数据库 | 新库不再创建 `daily_task_defs` / `daily_tasks` / `task_edit_sessions`；`SCHEMA_VERSION` 递增到 **13** |
| `/profile`、用户详情、使用统计 | 不再展示「今日任务 / 任务完成次数」 |
| 功能开关 | `tasks` 开关移除（其余 7 项不变：AI / 知识库 / 群规执法 / 游戏 / 签到 / 商城 / 兑换码） |
| 操作日志筛选 | 「✅ 任务」改为「🧩 功能开关」 |

### 升级提示

老库里的三张表**不再被读写**，留着不影响运行（也不需要迁移）；想彻底清干净，手动执行：

```sql
DROP TABLE IF EXISTS daily_task_defs;
DROP TABLE IF EXISTS daily_tasks;
DROP TABLE IF EXISTS task_edit_sessions;
```

### 顺带清理

- 🧹 管理面板主菜单、用户详情、使用统计、`/profile`、定时任务清理列表、文档与测试里所有任务相关引用全部移除
- 🧾 历史审计日志保留可读性：`task_*` 动作仍有中文标签，标了「（历史）」，只是不再单独分类
- 🧪 测试 214 → **193 个**：删除任务相关用例，其余（含出站消息 HTML 体检）全部通过

---

## v2.8.2 · 2026-09-12

> 🐛 修复「➕ 添加任务」点不动 —— 引导文案里的 `<兑换码>` 未转义，Telegram 拒收整条消息

### 🐛 修复

- ➕ **「添加任务」点不动的原因**：第 1 步文案把触发条件的提示原样塞进 HTML 消息，而兑换码那条写的是 `发送 /redeem <兑换码>（仅私聊）`——Telegram 把 `<兑换码>` 当成不支持的 HTML 标签，**直接拒收整条消息**（`can't parse entities: Unsupported start tag`）。返回值又被丢弃，于是现象就是「点了按钮没有任何下一步」
- 🔍 **引导步骤发送失败不再静默**：新增 `sendStep()`——失败会写一条审计日志（`task_step_send_failed`，含 Telegram 原始报错），并退化成不带按钮的纯文本再发一次；配合「回复编号 1-5 也能选触发条件」，按钮发不出去也能继续
- ⌨️ **在「选触发条件」那步打字不再毁掉流程**：以前输入会掉进「未知步骤 → 清会话 → 交给 AI」，现在会重新提示并保留会话

---

## v2.8.1 · 2026-09-12

> 🐛 一批健壮性修复：管理员不能封禁自己、引导会话互斥、HTML 转义补漏、负数扣分、删除场景二次确认

### 🐛 修复

- ⌨️ **群里填引导表单时输入被丢弃**：引导式输入只对私聊放行，管理员在群里点开「添加任务 / 编辑群规 / 添加知识库文档」后，紧接着输入的文本会被群聊过滤逻辑丢掉（带 @机器人 还会被当成 AI 对话扣积分）。现在群里也能继续填写
- ✅ **编辑任务后详情卡片不刷新**：流程结束时用 `editMessageText(..., null, ...)` 刷新卡片，Telegram 会返回 `message identifier is not specified`，管理员只看到「修改成功」却看不到最新卡片。现在没有 message_id 时改为新发一条
- 🚫 **管理员可以封禁自己**：场景编辑里的「🚫 封禁」没有自我保护。现在三层拦截——面板显示「管理员不可封禁」、服务层 `setUserBlocked` / `banUserById` 直接拒绝、封禁名单里不会出现管理员
- 🚫 **群规处置也可能打到管理员**：命令处置与成员举报补上目标校验，`executePunishment` 再兜一层，避免旧卡片把管理员封了
- 🧭 **引导会话互相抢消息**：同一会话残留两个引导状态时，先写入的那个会吞掉后续所有文本，表现为「点了按钮没反应」。现在开新流程前统一清掉其它会话（新增 `services/sessions.js`）
- 🔒 **HTML 转义补漏**：`/tasks` 的任务名称与提示、处置公告里的对象名 / 理由 / 依据都没有转义，只要出现 `<` 整条消息就会被拒收（群成员昵称是用户可控的）
- 🔢 **负数扣分会凭空加分**：`tryDeductPoints` 现在拒绝负数额度（0 仍然允许，用于免费商品）
- 🗑️ **删除场景没有二次确认**：点「🗑️ 删除场景」会立刻清空该场景的对话记忆与今日用量，现在改为先弹确认卡片
- 🧩 **回调分支更稳**：`admin_task_*` 兜底分支只认纯数字任务 ID，将来新增按钮不会被它截胡

### 🧪 测试与文档

- 测试 196 → **209 个**：新增 `test/bugfix.test.mjs`，逐条覆盖上面的修复
- `AGENTS.md` 补三条约定：引导会话必须互斥、机器人管理员不可被封禁/处置、`editMessageText` 不允许传 null message_id

---

## v2.8.0 · 2026-09-12

> ❗ 处置只认 `/指令`（移除自然语言入口）；新增「🗑️ 自动删除」面板，可按消息类型分别设置保留时长

### ❗ 破坏性变更

- ⌨️ **处置只认 `/指令`，移除自然语言入口**：以前群里 @机器人 说「封禁 @某人 发广告」就会弹卡片，但「封禁」「拉黑」「踢了」在日常聊天里太常见，容易被误当成执法指令。现在：
  - 处置统一走 `/ban`、`/kick`、`/groupban`、`/mute`、`/unban`、`/unmute`（可回复对方消息后直接用）
  - 举报改为 `/report 违规现象`（**先回复违规的那条消息**），不再识别「举报 xxx」
  - 申诉只认 `/appeal 理由`，私聊里说「申诉 …」不再触发
  - 群里说「封禁 xxx」只会当成普通对话（走 AI），不产生任何处置记录
- 👮 **本群管理员可用指令处置**：`/ban` `/unban` `/kick` `/groupban` `/mute` `/unmute` `/rules` 在群里对本群管理员开放（不需要 `/admin` 解锁）；`/guard` 与 `/setrules` 仍只给机器人管理员

### ✨ 新增功能

- 🗑️ **消息自动删除可设置，且区分消息类型**（管理控制台 → 🗑️ 自动删除）：群聊里机器人自己发的消息，按类型分别设定保留时长（**0 秒 = 不删除**）：

  | 类型 | 默认 | 覆盖的消息 |
  |------|------|-----------|
  | ⚙️ 指令回执 | 5 秒 | 指令结果、用法提示、错误提示 |
  | 🛡️ 执法回执 | 5 秒 | 封禁 / 踢出 / 禁言的结果提示 |
  | 🖲️ 确认卡片 | 保留 | 处置 / 申诉确认卡片（要留时间点） |
  | 🤖 AI 回复 | 保留 | 群聊里 AI 的回答 |
  | 📢 系统通知 | 保留 | 处置公告、到期提醒与每日概况 |

  - 两级设置：**本场景** 优先于 **全局默认**，可一键「跟随全局 / 恢复内置默认」
  - 群聊按**群**共享设置（`group:<群ID>`），不是每个成员一份；私聊消息本来就不删
  - 默认值与改造前完全一致，不改设置就没有行为变化

### 🐛 修复

- 🐛 **举报人可能确认自己的举报**：举报卡片的 `operator_id` 记的是举报人，而确认校验允许「发起人本人」；现在举报 / 预警卡片的 `operator_id` 留空，只有机器人管理员能确认
- 🐛 **本群管理员处置记错操作人**：指令处置把 `operator_id` 记成了机器人管理员，导致本群管理员点不了自己发起的卡片
- 🐛 **私聊卡片键盘丢失**：`sendAutoDelete` 在私聊分支只发纯文本、会丢掉 inline keyboard

### 🧪 测试与文档

- 测试 179 → **196 个**：新增 `test/auto-delete.test.mjs`；guard 测试改为指令入口，并补「自然语言不再触发处置 / 举报 / 申诉」「本群管理员用指令处置」「执法回执按类型取时长」等回归
- 同步 `README.md`、`docs/commands.md`、`docs/group-guard.md`、`docs/admin-console.md`、`docs/architecture.md`、`AGENTS.md`

---

## v2.7.1 · 2026-09-12

> 🐛 知识库不再「答不了却硬答」，无关资料也不再被塞进提示词

### 🐛 修复

- 🐛 **知识库「答不了却硬答」**：以前只要检索到资料，提示词就要求模型「资料里没有的就说『资料中未提及』」，于是 AI 明明能答也会拒答，甚至编出一个《无相关资料》的出处。现在：
  - 新增**回答模式**：默认 `hybrid`——资料能回答就优先依据资料并标注来源，**资料没覆盖时用 AI 自己的知识正常回答**；置 `KB_ANSWER_MODE=strict` 才恢复「只依据资料」的严格问答
  - 提示词明确要求**只能标注真实存在的资料标题**，没有引用资料时不要写来源
- 🐛 **无关资料被注入提示词**：余弦相似度只要过 0.30 就会被拼进提示词，导致「沾边但不相关」的资料把模型带偏。现在增加**相关度门槛**：综合分 ≥ 0.45 才算强相关，低于它必须有真实关键词重叠才采用

### 🔧 优化

- 🔍 知识库面板显示当前**回答模式**；「检索测试」列出全部候选（阈值置零）并同时显示「综合分 + 关键词分」

### 🧪 测试与文档

- 测试 175 → **179 个**：新增相关度门槛回归、回答模式解析、hybrid/strict 提示词，以及「没有相关资料时不应注入资料段落」
- `docs/knowledge-base.md` 增加「回答模式」与「检索测试怎么用」，`docs/configuration.md` 增加 `KB_ANSWER_MODE`

---

## v2.7.0 · 2026-09-12

> ✨ 群规执法补全闭环：到期通知、申诉、静默预警、一键撤销、群规版本；顺带增强知识库与用户详情

### ✨ 新增功能

- ⌛ **处置到期自动通知**：限时禁言 / 封禁到期时，定时任务在群里公告「处置已到期」，并私聊当事人「限制已解除」；日报汇总刚到期条数
- 🙋 **处置申诉通道**：被处置人私聊 `/appeal 理由`，申诉卡片发到管理员私聊，管理员「✅ 撤销处置」即解除限制并公告、「❌ 驳回申诉」通知当事人；同一处置重复申诉会被拦下
- 🔔 **主动预警（静默）**：群成员发言命中预警关键词时，**只私聊提醒管理员**（群里不发声），卡片直接带「确认执行 / 改处置」按钮；同一用户 10 分钟内只提醒一次
- ↩️ **一键撤销处置**：处置记录页每条可撤销记录都有「↩️ 撤销」，解除机器人封禁与群内限制、公告并私聊当事人，状态记 `revoked`
- 📇 **用户详情聚合页**：一屏看全身份、积分、签到、最近积分流水、最近订单、最近群内处置
- 🕘 **群规版本历史**：每次改群规自动留版本，可查看并一键回滚；只改默认处置不产生无意义版本
- 📚 **知识库增强**：
  - 支持 **.docx / .pdf** 上传（.docx 解 zip 取正文；PDF 尽力抽取文本层，扫不出文字会提示改用 OCR）
  - **🧠 重建索引**：补齐「上传时未绑定 AI」或「换过向量模型」的分块（面板每次 40 块，定时任务每次 20 块）
  - **生效范围可调**：群文档可「⬆️ 设为全局」，全局文档可「⬇️ 复制到本群」
- 🔍 **审计日志筛选**：按 用户 / 商城 / 执法 / 知识库 / 任务 / 系统 分类过滤，翻页保留条件

### 🗄️ 数据库

- 新增 `punishment_appeals`（申诉）、`group_rule_versions`（群规版本）；`group_guard` 增加 `alert_enabled` / `alert_keywords`，`kb_chunks` 增加 `model`；`SCHEMA_VERSION` → **12**

### 🧪 测试与文档

- 测试 151 → **175 个**：新增 `test/guard-extra.test.mjs` 与 `test/admin-extra.test.mjs`，并给新菜单补排版用例
- README 同步这些能力与新的数据库表、指令

---

## v2.6.0 · 2026-09-12

> ✨ 群规引导式编辑面板 + 输入框命令菜单 + 成员举报联动

### ✨ 新增功能

- 📜 **群规引导式编辑**（管理控制台 → 📜 群规执法，或群里的 `/guard`）
  - `📝 编辑群规` / `➕ 追加一条` / `🧹 清空群规`：全程对话式引导，30 分钟有效期，`/cancel` 可放弃
  - `⚖️ 默认处置`：机器人封禁 / 踢出 / 群内封禁 / 禁言，一键切换
  - `⏱️ 默认禁言时长`：10 分钟 / 30 分钟 / 1 小时 / 2 小时 / 1 天 / 永久
  - `✅/🚫 执法开关`、`📜 处置记录`（分页，含待确认 / 已执行 / 已取消 / 理由不成立 / 失败 / 已到期）
- ⌨️ **输入框命令待选**：聊天框输入 `/` 就能看到指令列表
  - 菜单**直接由命令注册表生成**，分三套：私聊、群聊（去掉「仅私聊」命令）、管理员私聊（含管理命令）
  - 部署后自动同步：内容哈希做版本标记，只有命令变了才调用 Telegram；同步放在 `waitUntil`
  - 管理员可用 `/syncmenu` 强制刷新
- 📣 **成员举报联动**：普通成员**回复违规消息**并 @机器人 说「举报 发广告」（v2.8.0 起改为 `/report`），理由校验通过后把带按钮的卡片**发到管理员私聊**，管理员一键在群里处置；群里同时给举报人回执

### 🔧 优化

- 🔗 **功能联动**：群规面板显示本群知识库文档数；处置记录与审计打通；定时任务新增 `guard_sessions` 清理
- ⌨️ 命令表新增 `/guard`、`/syncmenu`

### 🗄️ 数据库

- 新增 `guard_sessions`（群规引导状态），`SCHEMA_VERSION` → **11**

### 🧪 测试

- 测试 140 → **151 个**：新增 `test/command-menu.test.mjs`，并补群规面板与成员举报链路用例

---

## v2.5.0 · 2026-09-12

> ✨ 群规执法：群里一条指令处置违规成员，先校验理由、再确认卡片、全程可审计

### ✨ 新增功能

- 🛡️ **群规执法**（v2.8.0 起改为只认 `/指令`）

  ```
  /ban 发广告刷屏                 → 机器人封禁（私聊 + 所有群）
  /mute @某人 2小时 辱骂他人       → 群内限时禁言
  /kick @某人 发诈骗链接           → 踢出群组（可重新加入）
  /groupban @某人 发广告           → 群内封禁（不可重新加入）
  /unban @某人                     → 解除机器人封禁 + 群封禁
  ```

  - **必须说明理由**：依次比对「内置违规类型（广告 / 刷屏 / 辱骂 / 诈骗 / 色情 / 隐私…）→ 本群群规文本 → 本群知识库」，对不上就拒绝执行并列出可识别类型
  - **两种处置通道**：机器人功能封禁（写 `users.blocked`）；群组处置走 Telegram（踢出 / 群内封禁 / 限时禁言）
  - **确认卡片**：显示对象、处置、理由、依据，可一键改成其他处置方式或取消
  - 三种指认方式：**回复对方消息**、`@用户名`、直接写用户 ID
  - 权限：机器人管理员 或 本群管理员；执行群组处置要求机器人是本群管理员且有「封禁用户」权限
  - 全程写入 `group_punishments` 审计
- ⌨️ **配套指令**：`/kick`、`/groupban`、`/mute`、`/unmute`、`/rules`、`/setrules`，以及群里也可用的 `/ban`、`/unban`
- 📜 **群规管理**：`/setrules 群规正文` 写入本群群规，`/rules` 查看群规与可识别违规类型

### 🗄️ 数据库

- 新增 `group_guard`（群规 + 默认处置 + 默认禁言时长）、`group_punishments`（处置记录，含待确认状态），`SCHEMA_VERSION` → **10**；定时任务清理过期未确认的处置、标记到期禁言

### ⚙️ 功能开关

- 「群规执法」加入三级开关（全局 / 群聊场景 / 私聊场景）

### 🧪 测试

- 测试 126 → **140 个**：新增 `test/guard.test.mjs`（时长解析、动作识别、目标解析、理由校验、权限拒绝、确认 / 取消 / 改处置），并补执法卡片排版用例

---

## v2.4.0 · 2026-09-12

> ✨ 知识库（RAG）：让 AI 先查资料再回答，把机器人从「闲聊」变成「群助手」

### ✨ 新增功能

- 📚 **知识库（RAG）**
  - 上传资料（引导式粘贴文本，或直接发 `.txt` / `.md`），AI 回答前先检索命中的资料再作答，并标注来源
  - **群知识库 + 全局知识库**：群里打开 `/admin → 📚 知识库` 管理本群资料（`scope_key = group:<群ID>`），私聊里管理全局资料（`scope_key = global`）；检索时两者一起参与
  - 向量走 Workers AI（默认 `@cf/baai/bge-m3`，可用 `KB_EMBED_MODEL` 覆盖），以 base64(Float32Array) 存进 D1，Worker 内算余弦相似度；**未绑定 AI 时自动退化为中文二元组关键词检索**
  - 管理面板：➕ 添加文档 · 📄 文档列表（启用 / 停用 / 删除）· 🔍 检索测试（看命中与评分）
  - 容量保护：单个作用域默认最多 400 块，超出拒绝入库并提示精简
- 👥 **群组用户改为「群列表 → 群成员」两级浏览**：先看所有群（群 ID + 人数），点进去看成员，成员点进即场景编辑
- 🚫 **封禁名单**：新增「用户管理 → 🚫 封禁名单」（列表 + 一键解封），以及 `/ban <用户ID> [原因]`、`/unban <用户ID>`；封禁是用户级的，私聊与所有群同时生效

### 🗄️ 数据库

- 新增 `kb_docs`、`kb_chunks`（分块 + 向量）、`kb_sessions`（引导状态，30 分钟过期），`SCHEMA_VERSION` → **9**；定时任务新增 `kb_sessions` 兜底清理

### 🐛 修复

- 🐛 `banUserById` / `unbanUserById` 漏导入 `buildUserKey`，会直接抛 `ReferenceError`（被新增测试当场抓出）
- 🐛 **「重复解封」检测不到**：SQLite 的 `changes` 统计的是命中行数（`blocked=0` 再设 0 也算 1 行），改为先查状态再更新，并区分「用户不存在」与「不在封禁名单」

### 🧪 测试

- 测试 102 → **126 个**：新增 `test/knowledge.test.mjs`（分块、向量编解码、余弦、关键词兜底、入库 / 检索 / 停用 / 删除、容量保护、AI 注入资料、文件上传）与封禁 / 群组两级浏览用例

---

## v2.3.1 · 2026-09-12

> 🔧 用户管理收敛为二级菜单，主菜单不再被两排「用户」入口占满

### 🔧 变更

- 👥 **用户管理改成二级菜单**：主菜单原来是「💬 私聊用户 / 👥 群聊用户」两个入口，现在收敛成一个 `👥 用户管理`，点进去再选；用户列表底部的返回按钮改为 `🔙 返回用户管理`，不会一下跳回主菜单
- 📐 主菜单按钮由 10 个减到 9 个，仍是两列网格：私聊 6 行、群聊 5 行，排版在 8 行以内

### 🧪 测试

- 测试 100 → **102 个**：新增「用户管理二级菜单排版」与「点击进入二级菜单」用例，并断言私聊 / 群组入口不再出现在主菜单

---

## v2.3.0 · 2026-09-12

> 🔧 统一菜单排版工具，并修掉一批「会话吞消息 / 并发 / 时区」类问题

### 🏗️ 新增与重构

- 📐 **统一的菜单排版工具 `src/utils/layout.js`**：`grid`（两列网格）、`compactLabel`（按码点截断，不切坏 emoji）、`clampPage` / `totalPagesOf` / `pageOffset` / `pagerRow`（分页）、`validateKeyboard`（约束校验）；所有会随数据量增长的菜单都改为调用它
- 📄 **菜单分页补齐**：商城商品列表、订单列表、用户列表都做了页码收敛（越界自动回最后一页）与行数控制（最多 8 行）
- 📂 **商城分类集中到 `src/shop/categories.js`**（原先 4 个文件各写一份映射）
- 🧩 **限额 / 冷却解析集中到 `services/users.js`**（`parseMaxDaily` / `parseRateLimit`）
- 🗄️ **SQL 拆分逻辑共用**：`splitSchemaStatements` 同时被生产建表与测试用 D1 替身复用，并支持行尾注释

### 🐛 修复

- 🐛 **引导式输入会话会一直吞消息**：`shop_add_sessions` / `shop_edit_sessions` 缺少有效期，管理员点开后走开，之后的普通消息会被当成表单内容。现在统一 30 分钟过期 + 定时任务兜底
- 🐛 **群聊指令被同步阻塞 5 秒**：`sendAutoDelete` 取不到 `waitUntil`，走了 `await sleep(5000)`；现在解析出真正的 Worker ctx，删除放进后台任务
- 🐛 **兑换码过期判断时区不一致**：列表页用 UTC、兑换时用 `APP_TIMEZONE`，凌晨会出现「列表说已过期、其实还能用」。统一走 `getDateKey(env)`
- 🐛 **AI 扣分流水余额可能不准**：改为使用扣分时返回的余额，不再二次查库
- 🐛 **签到加分失败没有回滚**：现在会回滚签到记录，避免「显示已签到但没拿到积分」
- 🐛 **群聊指令误删他人 @ 提及**：`/broadcast@Bot @某人` 不再把 `@某人` 一起删掉
- 🐛 **商城限购并发兜底**：下单后按真实订单数复核，超出的那一单自动取消并退款
- 🐛 **多处菜单单行 3~4 个按钮**：积分管理、限额、频率、自定义下注、游戏主界面、商品编辑面板统一改为两列
- 🔧 **`Math.random` 违反项目约定**：Telegram 重试抖动改用加密随机数

### 🧪 测试与文档

- 测试 79 → **100 个**：新增 `test/regression.test.mjs`（会话过期、群聊不阻塞、签到回滚、边界解析、积分夹断），`test/layout.test.mjs` 扩展到覆盖全部动态菜单
- 同步 README 与 `AGENTS.md`（排版与引导会话 TTL 约定），版本号同步到 `package.json` / `package-lock.json`

---

## v2.2.0 · 2026-09-12

> ✨ 功能开关恢复三级结构；管理面板全面两列化

### ✨ 新增与恢复

- ⚙️ **功能开关恢复三级**：🌍 全局设置 → 👥 群聊场景 → 💬 私聊场景，生效顺序「场景显式设置 → 全局设置 → 默认开启」，场景页支持「🔄 全部恢复跟随全局」；入口在管理控制台与场景管理菜单
- 📐 **菜单排版优化**：管理员主菜单改两列网格（9 个入口压到 6 行）；功能开关、兑换码列表、私聊用户列表统一两列（长名字自动截断）；场景管理按钮文案精简；用户列表每页 5 → 8 条

### 🐛 修复

- 🗄️ 移除 v2.1.0 里那条 `DELETE FROM scene_settings WHERE scene_key <> 'global'` 迁移——它会**在每次冷启动清空场景级开关**；`SCHEMA_VERSION` → **8**，并补回归测试锁住行为

### 🧪 测试

- 测试 71 → **79 个**：三级开关（全局 / 场景覆盖 / 单个清除 / 批量恢复）、场景开关不被迁移清除、菜单排版三类用例

---

## v2.1.1 · 2026-09-12

> 🔧 Schema 加版本标记，冷启动不再每次跑几十条建表语句

### 🔧 修复与优化

- 🗄️ **Schema 引导加版本标记**：新增 `SCHEMA_VERSION` 与 `schema.version` 记录，冷启动先读版本，已是最新就跳过建表与迁移（原来每次冷启动要跑 42 条语句，D1 对单次调用查询数有限制）
- 🧪 新增两个用例：版本写入 / 已是最新时跳过建表、冷启动只查一次库（共 **71 个**）
- 📄 新增 `AGENTS.md`：记录协作约定（不提交密钥、本地部署、提交前自检、Schema 变更流程、`github.com` 不可达时的 API 推送兜底等）

---

## v2.1.0 · 2026-09-12

> ✨ 每日任务可引导式增删改（**v2.9.0 已整体下线**）；功能开关收敛为全局

### ✨ 新增功能

- ✅ **每日任务可引导式增删改**（v2.9.0 已下线）：添加走「触发条件 → 名称 → 提示 → 奖励」，支持改名称 / 提示 / 奖励、停用启用、删除（二次确认）；同一触发条件可挂多个任务；**全勤奖可配置**（0 = 不发）
- 🗄️ 新增全局设置读写服务 `src/services/settings.js`，功能开关与任务设置统一走它

### 🔧 变更

- ⚙️ **功能开关收敛为全局**：入口只剩 `/admin → ⚙️ 功能开关（全局）`，场景编辑里的「本场景功能开关」移除（v2.2.0 又恢复成三级）

### 🐛 修复与优化

- 🎟️ 使用兑换码也会触发「使用一次兑换码」类任务（新增 `redeem` 触发条件）
- 🗃️ 新增 `daily_task_defs`、`task_edit_sessions` 两张表；默认任务用一次性标记 `task.seeded` 灌入，**管理员删掉的任务不会在下次启动复活**
- 🧪 测试 65 → **69 个**：任务 CRUD、多任务同触发、删除后进度保留、引导式添加 / 修改的入口链路

---

## v2.0.0 · 2026-09-12

> 🏗️ 从「脚本集合」升级成「可配置平台」：命令、功能、玩法都能配置

### 🏗️ 架构

- 🧭 **声明式命令注册表**：所有命令集中定义在 `src/handlers/commands/registry.js`（名称 / 别名 / 权限 / 仅私聊 / 功能开关 / 说明）
  - `message.js` 不再维护一长串 `if` 分支，只保留消息解析与 AI 兜底
  - `/help` **由注册表自动生成**，按「普通 / 管理员」「私聊 / 群聊」裁剪
  - 新增命令只需加一条注册项，权限、群聊拦截、功能开关、帮助文案自动生效

### ✨ 新增功能

- ✅ **每日任务**：`/tasks`（别名 `/daily`）——签到 / 和 AI 聊一次 / 玩一局游戏 / 在商城兑换一次，完成即发奖，全部完成再发全勤奖（每天一次并推送祝贺）。**v2.9.0 已下线**
- ⚙️ **功能开关**：AI 对话 / 游戏大厅 / 每日签到 / 积分商城 / 兑换码 / 每日任务 均可单独开关（全局 + 单场景覆盖）
- 🛒 **商品限购**：`shop_items.per_user_limit`（0 = 不限），下单前校验已购数量，**已取消订单不占名额**；商品详情与 `/shop_edit` 都可查看 / 修改

### 🐛 修复与优化

- 🗃️ 新增 `scene_settings`、`daily_tasks` 两张表，`shop_items` 新增 `per_user_limit` 列（首次请求自动迁移）
- 🏷️ 补齐 v1.3.0 遗漏的审计文案（兑换码生成 / 启停、功能开关），历史 `shop_order_ship` 记录标注为旧版
- 🧪 测试 46 → **65 个**：新增功能开关、每日任务、命令注册表三组用例

---

## v1.3.1 · 2026-09-12

> ❗ 商城只保留虚拟物品与服务，去掉「发货」环节

### 🔧 变更

- 🚫 **移除「实物商品」分类**：只支持 **虚拟物品** 与 **服务**；旧数据里的 `physical` 会在首次请求自动归入虚拟物品
- 🚫 **移除「发货」环节**：订单状态机简化为 `待处理 → 已完成 / 已取消`；「🚚 标记已发货」改为「✅ 标记已完成（已发放）」，历史 `shipped` 订单自动迁移
- 🔔 **标记完成会通知用户**：管理员确认发放后，用户收到「订单已完成」（含订单号、商品与备注）
- 🧾 下单备注定位调整为「服务类商品的补充说明」（想要的款式、联系方式、兑换要求等）

### 🧪 测试

- 新增分类与订单状态的数据迁移测试，商城用例改用 `done` 状态，共 46 个用例

---

## v1.3.0 · 2026-09-12

> ✨ 兑换码 · 下单备注 · 定时任务 · 第一套真正的测试套件

### ✨ 新增功能

- 🎟️ **兑换码**：管理员 `/code_new <积分> [次数] [有效天数]` 生成形如 `TG7KQ2M4XZ` 的码，用户在私聊 `/redeem <码>` 领积分
  - 每个码**每人限兑一次**（数据库唯一约束兜底），可选总次数上限（`0` = 不限）与过期日期
  - `/code_list` 或管理控制台「🎟️ 兑换码」查看已用 / 上限并随时停用、启用
- 🧾 **下单备注**：商品详情页新增「✍️ 填写备注」，备注随订单写入 `remark`，同时出现在新订单通知和订单详情里
- ⏰ **定时任务（Cron Triggers）**：每天清理过期管理员会话、陈旧群发 / 引导 / 备注草稿，停用过期兑换码，并给管理员推送「昨日概况 + 最早几笔待处理订单」日报
- 🧪 **测试套件**：`npm test`（`node:test` + `node:sqlite` 内存库跑真实 SQL）覆盖表结构与迁移、连续签到、积分边界、兑换码全状态、商城取消退款幂等、下单备注、定时任务清理与统计、AI 上下文裁剪、入口链路

### 🐛 修复与优化

- 🧠 AI 上下文裁剪从 `handlers/ai.js` 抽成 `src/services/history.js`，可单测
- 🗂️ 备注输入期间发指令不会被误当成备注；进入输入态 30 分钟未回复自动失效；下单失败时草稿保留
- 🗃️ 新增 `redeem_codes`、`redeem_logs`、`shop_order_drafts` 表（首次请求自动创建，老库无需手工 SQL）
- ⚙️ `wrangler.toml` 增加 `[triggers] crons = ["0 16 * * *"]`（UTC，等于北京时间 00:00）
- 🧹 `npm run check` 现在也检查 `test/`、`test-helpers/` 的语法与 import 路径

---

## v1.2.0 · 2026-09-12

> ✨ 商城与积分的日常运营功能一次性补齐

### ✨ 新增功能

- ✏️ **管理员引导式编辑商品**：`/shop_edit <商品ID>` 或「商品详情 → ✏️ 编辑」，可改名称、价格、库存、分类、图标、说明
- ❌ **用户自助取消订单**：`/orders` 与商城「我的订单」里，待处理订单可一键取消并退款（库存同步回滚）
- 📜 **积分流水分页**：`/points` 支持上一页 / 下一页
- 🏆 **积分排行榜**：`/rank` 显示 Top 10 并标注自己的排名
- 📅 **连续签到递增奖励**：首日 +5，逐日 +1 上限 +20，每满 7 天额外 +20，断签重置
- 📢 **管理员群发**：`/broadcast <内容>` 二次确认后分批推送给所有私聊用户，支持断点续发
- 🚫 **封禁 / 解封用户**：被封禁用户无法使用 AI、游戏、商城等任何功能
- 📋 **管理员操作日志**：记录谁在什么时候改了什么，控制台可翻页审计
- 🧹 **记忆管理**：`/clearmem <群ID> [用户ID]` 或场景编辑面板「清空此场景记忆」
- 💾 **D1 备份脚本**：`npm run backup` / `npm run backup:local`，输出到 `backups/`
- 🔐 **生产配置私有备份**：`npm run backup:config` 通过 GitHub API 把 `wrangler.production.toml` 与 `.dev.vars` 备份到私有仓库（上传前校验目标仓库必须为私有）
- 🤖 **AI 模型自动回退**：主模型失败或返回空内容时自动切换备选模型
- 🧮 **AI 上下文长度管理**：历史按字符预算（默认 6000）截断，单条消息也会截断
- 🔁 **Telegram 429 退避重试**：限流 / 5xx / 网络抖动自动退避重试（最多 3 次）
- ✅ **本地自检脚本**：`npm run check` 检查全部文件语法与相对 import 路径
- ⬆️ **wrangler 升级到 4.x**：修复依赖漏洞告警，部署产物与 3.x 一致
- 🌍 **代码与生产配置分离**：公开仓库只保留代码与占位符模板，真实配置留在被 gitignore 的 `wrangler.production.toml`

### 🐛 修复与优化

- 🧹 **移除 GitHub Actions 工作流**：本仓库定位是「代码备份 + 开源」，部署统一在本地用 `npm run deploy:prod`；不在公开仓库里挂部署凭据
- 📄 许可统一为 **GPL-3.0**：README 的「许可」章节此前误写为 MIT，已改正并在 `package.json` 补 `"license": "GPL-3.0-or-later"`
- 📖 重写 README：重排章节结构，补齐新指令、配置项、数据库表、备份 / 安全与常见问题
- 🔒 配置备份脚本改为从本地未跟踪文件 `.config-backup` 读取目标仓库
- 🧾 订单取消改为原子状态流转（`WHERE status = 'pending'`），避免并发重复退款
- 🗂️ 商城模块新增 `shop/add.js`、`shop/edit.js`，用户与管理员共用同一套退款逻辑
- 🗃️ 新增 `shop_edit_sessions`、`admin_logs`、`broadcast_drafts` 表与 `users.blocked` 字段（自动迁移）
- 📄 `/orders` 与商城订单页合并为同一套渲染逻辑
- 🧠 AI 请求数组不再被后续流程改写，落库历史与请求内容解耦

---

## v1.1.0 · 2026-09-12

> ✨ 商品可以引导式添加了，商城开始分页

### ✨ 新增功能

- ➕ 管理员引导式添加商品
- 🔐 新增可选 `WEBHOOK_SECRET` webhook 安全校验
- 📄 商城商品分页

### 🐛 修复与优化

- 🧾 管理员调整积分时，流水记录实际变化量
- 🎯 群聊指令目标识别，自动忽略发给其他机器人的指令
- 🖼️ 欢迎页与个人卡片排版优化，管理员 / 普通用户差异化展示
- 🐛 修复 Telegram 指令描述中文显示为 `???` 的问题

---

## v1.0.0 · 2026-09-12

> 🎉 首个版本：Telegram AI Bot 上线 Cloudflare Workers

### ✨ 功能

- 🤖 AI 对话（Cloudflare Workers AI，Llama 3.3 70B）
- 🪙 全局积分系统与每日签到
- 🎮 游戏大厅：骰子猜大小、欢乐老虎机、抛硬币、幸运转盘
- 🛒 积分商城：商品浏览、兑换、我的订单、库存管理
- 👑 管理控制台：用户 / 场景管理、积分调整、每日限额与发送频率设置
