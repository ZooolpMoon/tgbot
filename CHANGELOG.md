# 更新日志

## v2.8.1 — 2026-09-12

### 修复

- ✅ **每日任务在群里改不了**（新增 / 编辑全部失效）：引导式输入只对私聊放行，在群里点「➕ 添加任务」后提示能收到，但紧接着输入的文本会被群聊过滤逻辑丢掉（带 @机器人 还会被当成 AI 对话扣积分）。现在群里也能走完「选触发条件 → 名称 → 提示 → 奖励」的引导，编辑任务字段同理
- ✅ **编辑任务后详情卡片不刷新**：引导流程结束后用 `editMessageText(..., null, ...)` 刷新卡片，Telegram 会返回 `message identifier is not specified`，管理员只看到「修改成功」而看不到最新卡片。现在没有 message_id 时改为新发一条
- 🚫 **管理员可以封禁自己**：用户管理 → 场景编辑里的「🚫 封禁」没有任何自我保护，点下去会把自己写进封禁名单。现在三层拦截——面板显示「管理员不可封禁」、服务层 `setUserBlocked` / `banUserById` 直接拒绝、封禁名单里不会出现管理员
- 🚫 **群规处置也可能打到管理员**：命令处置与成员举报补上目标校验，`executePunishment` 再兜一层，避免旧卡片把管理员封了
- 🧭 **引导会话互相抢消息**：同一会话残留两个引导状态时（例如先点了「添加知识库文档」又去点「添加任务」），先写入的那个会吞掉后续所有文本，表现为「点了按钮没反应」。现在开新流程前统一清掉其它会话（新增 `services/sessions.js`，商城添加/编辑、任务、知识库、群规、下单备注全部接入）
- 🔒 **HTML 转义补漏**：`/tasks` 的任务名称与提示、处置公告里的对象名 / 理由 / 依据都没有转义，只要出现 `<` 整条消息就会被 Telegram 拒绝（群成员昵称是用户可控的，普通成员也能触发）
- 🔢 **负数扣分会凭空加分**：`tryDeductPoints` 现在拒绝负数额度（0 仍然允许，用于免费商品）
- 🗑️ **删除场景没有二次确认**：点「🗑️ 删除场景」会立刻清空该场景的对话记忆与今日用量，现在改为先弹确认卡片
- 🧩 **回调分支更稳**：`admin_task_*` 兜底分支只认纯数字任务 ID，将来新增的按钮不会被它截胡

### 测试与文档

- 🧪 测试 196 → **209 个**：新增 `test/bugfix.test.mjs`，逐条覆盖上面的修复（含「群里 @机器人 输入走引导而不是 AI」「管理员不可被封禁」等回归）
- 📝 `AGENTS.md` 补三条约定：引导会话必须互斥、机器人管理员不可被封禁/处置、`editMessageText` 不允许传 null message_id

## v2.8.0 — 2026-09-12

### 破坏性变更

- ⌨️ **处置只认 `/指令`，移除自然语言入口**：以前群里 @机器人 说「封禁 @某人 发广告」就会弹处置卡片，但「封禁」「拉黑」「踢了」「闭嘴」这些词在日常聊天里太常见，容易被误当成执法指令。现在：
  - 处置统一走 `/ban`、`/kick`、`/groupban`、`/mute`、`/unban`、`/unmute`（可回复对方消息后直接用，不必写名字）
  - 举报改为 `/report 违规现象`（**先回复违规的那条消息**），不再识别「举报 xxx」
  - 申诉只认 `/appeal 理由`，私聊里说「申诉 …」不再触发
  - 群里说「封禁 xxx」这类话只会当成普通对话（走 AI），不会产生任何处置记录
- 👮 **本群管理员用指令处置**：`/ban` `/unban` `/kick` `/groupban` `/mute` `/unmute` `/rules` 在群里对本群管理员（creator / administrator）开放，不需要机器人管理员的 `/admin` 解锁；`/guard` 与 `/setrules` 仍只给机器人管理员

### 新增功能

- 🗑️ **消息自动删除可设置，且区分消息类型**（管理控制台 → 🗑️ 自动删除）：群聊里机器人自己发的消息，可以按类型分别设置保留时长（**0 秒 = 不删除**）：

  | 类型 | 默认 | 覆盖的消息 |
  |------|------|-----------|
  | ⚙️ 指令回执 | 5 秒 | 指令结果、用法提示、错误提示 |
  | 🛡️ 执法回执 | 5 秒 | 封禁 / 踢出 / 禁言的结果提示 |
  | 🖲️ 确认卡片 | 保留 | 处置 / 申诉确认卡片（需要留时间点击） |
  | 🤖 AI 回复 | 保留 | 群聊里 AI 的回答 |
  | 📢 系统通知 | 保留 | 处置公告、到期提醒与每日概况 |

  - 两级设置：**本场景** 优先于 **全局默认**，面板可一键「跟随全局 / 恢复内置默认」
  - 群聊按**群**共享设置（`group:<群ID>`），不是每个成员一份；私聊消息本来就不删
  - 默认值与改造前完全一致，不改设置就不会有行为变化

### 修复

- 🐛 **举报人可能确认自己的举报**：举报卡片的 `operator_id` 以前记的是举报人，而确认校验允许「发起人本人」，等于举报人拿到按钮就能直接处置别人（卡片虽发在管理员私聊）；现在举报 / 预警卡片的 `operator_id` 留空，只有机器人管理员能确认
- 🐛 **本群管理员处置记错操作人**：指令处置把 `operator_id` 记成了机器人管理员，导致本群管理员点不了自己发起的确认卡片
- 🐛 **私聊卡片键盘丢失**：`sendAutoDelete` 在私聊分支只发纯文本、会丢掉 inline keyboard（新版自动删除链路引入的隐患，已一并修掉）

### 测试与文档

- 🧪 测试 179 → **196 个**：新增 `test/auto-delete.test.mjs`（类型默认值、两级设置、作用域归一化、按类型取时长、0 = 不删除、面板读写与恢复默认），guard 测试改为指令入口并补「自然语言不再触发处置 / 举报 / 申诉」「本群管理员用指令处置」「执法回执按类型取时长」等回归
- 📝 同步 `README.md`、`docs/commands.md`、`docs/group-guard.md`、`docs/admin-console.md`、`docs/architecture.md`、`AGENTS.md`

## v2.7.1 — 2026-09-12

### 修复

- 🐛 **知识库「答不了却硬答」**：以前只要检索到资料，提示词就会要求模型「资料里没有的就说『资料中未提及』」，于是即使 AI 本来能回答，也会回一句「资料中未提及」，甚至编出一个《无相关资料》的出处。现在：
  - 新增**回答模式**：默认 `hybrid` —— 资料能回答就优先依据资料并标注来源，**资料没覆盖时用 AI 自己的知识正常回答**；置 `KB_ANSWER_MODE=strict` 才恢复「只依据资料」的严格问答
  - 提示词明确要求**只能标注真实存在的资料标题**，没有引用资料时不要写来源，禁止编造《…》
- 🐛 **无关资料被注入提示词**：余弦相似度只要过 0.30 就会被拼进提示词，导致「沾边但不相关」的资料把模型带偏。现在增加**相关度门槛**：综合分 ≥ 0.45 才算强相关，低于它必须有真实关键词重叠才采用

### 优化

- 🔍 知识库面板显示当前**回答模式**；「检索测试」会列出全部候选（阈值置零）并同时显示「综合分 + 关键词分」，方便判断资料够不够

### 测试与文档

- 🧪 测试 175 → **179 个**：新增相关度门槛回归、回答模式解析、hybrid/strict 提示词，以及「知识库没有相关资料时不应注入资料段落」
- 📝 `docs/knowledge-base.md` 增加「回答模式」与「检索测试怎么用」，`docs/configuration.md` 增加 `KB_ANSWER_MODE`，FAQ 增加两条对应说明

## v2.7.0 — 2026-09-12

### 新增功能

- ⌛ **处置到期自动通知**：限时禁言/封禁到期时，定时任务在群里公告「处置已到期」，并私聊当事人「限制已解除」；日报里也会汇总刚到期的条数
- 🙋 **处置申诉通道**：被处置人可以私聊机器人说「申诉 理由」（或 `/appeal 理由`），机器人把申诉卡片发到管理员私聊，管理员点「✅ 撤销处置」即解除限制并在群里公告，点「❌ 驳回申诉」会通知当事人；同一处置重复申诉会被拦下
- 🔔 **主动预警（静默）**：群成员发言命中预警关键词时，**只私聊提醒管理员**（群里不发声），卡片上直接带「确认执行 / 改处置」按钮；同一用户 10 分钟内只提醒一次，管理员与群管理员发言不预警
- ↩️ **一键撤销处置**：处置记录页每条可撤销的处置都有「↩️ 撤销」按钮，会解除机器人封禁与群内限制、公告并私聊当事人，状态记为 `revoked`
- 📇 **用户详情聚合页**：场景编辑 → 📇 用户详情，一屏看全身份、积分、签到、今日任务、最近积分流水、最近订单、最近群内处置
- 🕘 **群规版本历史**：每次改动群规自动留版本，面板可看历史并一键回滚；只改默认处置不会产生无意义的版本
- 📚 **知识库增强**：
  - 支持 **.docx / .pdf** 上传（.docx 解 zip 取正文；PDF 尽力抽取文本层，扫不出文字时会明确提示改用 OCR）
  - **🧠 重建索引**：补齐「上传时未绑定 AI」或「换过向量模型」的分块，面板按钮每次处理 40 块，定时任务每次 20 块
  - **生效范围可调**：群文档可「⬆️ 设为全局」，全局文档可「⬇️ 复制到本群」（内容与向量一起复制）
- 🔍 **审计日志筛选**：按 用户 / 商城 / 执法 / 知识库 / 任务 / 系统 分类过滤，翻页时保留筛选条件

### 数据库

- 🗄️ 新增 `punishment_appeals`（申诉）、`group_rule_versions`（群规版本）；`group_guard` 增加 `alert_enabled` / `alert_keywords`，`kb_chunks` 增加 `model`（记录向量模型，便于精准重建）；`SCHEMA_VERSION` 递增到 **12**

### 测试与文档

- 🧪 测试 151 → **175 个**：新增 `test/guard-extra.test.mjs`（到期通知、申诉批准/驳回/重复、静默预警、撤销处置、群规版本与回滚）与 `test/admin-extra.test.mjs`（用户详情、日志筛选、索引重建、范围调整、docx/pdf 解析、带说明文字的文档上传），并给新菜单补了排版用例
- 📝 README 同步这些能力与新的数据库表、指令

## v2.6.0 — 2026-09-12

### 新增功能

- 📜 **群规引导式编辑**（管理控制台 → 📜 群规执法，或群里的 `/guard`）
  - `📝 编辑群规` / `➕ 追加一条` / `🧹 清空群规`：全程对话式引导，发一段文字就写进去（30 分钟有效期，`/cancel` 可放弃）
  - `⚖️ 默认处置`：机器人封禁 / 踢出 / 群内封禁 / 禁言，一键切换（下指令没指明就用它）
  - `⏱️ 默认禁言时长`：10 分钟 / 30 分钟 / 1 小时 / 2 小时 / 1 天 / 永久
  - `✅/🚫 执法开关`：一键启停本群执法
  - `📜 处置记录`：分页查看本群处置（待确认 / 已执行 / 已取消 / 理由不成立 / 失败 / 已到期）
- ⌨️ **输入框命令待选**：在聊天框输入 `/` 就能看到指令列表
  - 菜单**直接由命令注册表生成**，加新命令不用改别的地方；分三套：私聊（普通命令）、群聊（去掉「仅私聊」命令）、管理员私聊（含管理命令）
  - 部署后自动同步：用内容哈希做版本标记，只有命令变了才调用 Telegram；同步放在 `waitUntil`，不拖慢请求
  - 管理员可用 `/syncmenu` 强制刷新
- 📣 **成员举报联动**：普通成员**回复违规消息**并 @机器人 说「举报 发广告」，理由校验通过后，机器人把带「确认执行 / 改处置」按钮的卡片**发到管理员私聊**，管理员一键就能在群里处置；群里同时给举报人回执

### 优化

- 🔗 **功能联动**：群规面板显示本群知识库文档数（理由校验会用知识库）；处置记录与审计打通；定时任务新增 `guard_sessions` 清理
- ⌨️ 命令表新增 `/guard`、`/syncmenu`

### 数据库

- 🗄️ 新增 `guard_sessions`（群规引导式编辑状态），`SCHEMA_VERSION` 递增到 **11**

### 测试与文档

- 🧪 测试 140 → **151 个**：新增 `test/command-menu.test.mjs`（菜单生成、名称描述合法性、哈希版本、三套作用域、isolate 只检查一次），并补上群规面板（改默认处置 / 时长 / 开关 / 引导式编辑 / 处置记录）与成员举报链路用例

## v2.5.0 — 2026-09-12

### 新增功能

- 🛡️ **群规执法**：管理员在群里 @机器人 就能用自然语言下达处置命令，机器人先按群规校验理由，再弹确认卡片，确认后才执行
  ```
  @YourBot 封禁 @某人 发广告刷屏        → 机器人封禁（私聊 + 所有群）
  @YourBot 把 @某人 禁言 2小时 辱骂他人 → 群内限时禁言
  @YourBot 踢出 @某人 发诈骗链接        → 踢出群组（可重新加入）
  @YourBot 群内封禁 @某人 发广告        → 群内封禁（不可重新加入）
  @YourBot 解封 @某人                   → 解除机器人封禁 + 群封禁
  ```
  - **必须说明理由**：理由会依次比对「内置违规类型（广告/刷屏/辱骂/诈骗/色情/隐私…）→ 本群群规文本 → 本群知识库」，对不上就拒绝执行并列出可识别类型
  - **两种处置通道**：机器人功能封禁（写 `users.blocked`，机器人停止服务该用户）；群组处置走 Telegram（踢出 / 群内封禁 / 限时禁言，支持 30分钟 / 2小时 / 1天 / 永久）
  - **确认卡片**：显示对象、处置、理由、依据，可一键改成其他处置方式，也可直接取消
  - 支持三种指认目标的方式：**回复对方消息**、`@用户名`（需对方与机器人交互过）、直接写用户 ID
  - 权限：机器人管理员 或 本群管理员（creator/administrator）才能下达；执行群组处置要求机器人是本群管理员且有「封禁用户」权限
  - 全程写入 `group_punishments` 审计，可追溯谁在什么时候因为什么处置了谁
- ⌨️ **配套指令**：`/kick`、`/groupban`、`/mute`、`/unmute`、`/rules`、`/setrules`，以及群内也可用的 `/ban`、`/unban`
- 📜 **群规管理**：`/setrules 群规正文` 写入本群群规，`/rules` 查看群规与可识别的违规类型

### 数据库

- 🗄️ 新增 `group_guard`（每群群规 + 默认处置 + 默认禁言时长）、`group_punishments`（处置记录，含待确认状态），`SCHEMA_VERSION` 递增到 **10**；定时任务会清理过期未确认的处置、标记到期禁言

### 新增功能开关

- ⚙️ 「群规执法」加入三级功能开关，可按全局 / 群聊场景 / 私聊场景关闭

### 测试与文档

- 🧪 测试 126 → **140 个**：新增 `test/guard.test.mjs`（时长解析、动作识别、三种目标解析、理由校验、权限拒绝、确认/取消/改处置、机器人无权限时的保护、功能开关），并给执法卡片补了排版用例
- 📝 README 新增「🛡️ 群规执法」章节与指令表、数据库表、目录结构更新

## v2.4.0 — 2026-09-12

### 新增功能

- 📚 **知识库（RAG）**：把 bot 从「闲聊」升级成「群助手」
  - 管理员上传资料（引导式粘贴文本，或直接发 `.txt` / `.md` 文件），AI 回答前先检索命中的资料再作答，并标注来源
  - **群知识库 + 全局知识库**：在群里打开 `/admin → 📚 知识库` 管理本群资料（`scope_key = group:<群ID>`），在私聊里打开管理全局资料（`scope_key = global`）；检索时两者一起参与
  - 向量走 Workers AI（默认 `@cf/baai/bge-m3`，可用 `KB_EMBED_MODEL` 覆盖），向量以 base64(Float32Array) 存进 D1，Worker 内算余弦相似度；**未绑定 AI 时自动退化为中文二元组关键词检索**
  - 管理面板：➕ 添加文档 · 📄 文档列表（启用/停用/删除，二次确认）· 🔍 检索测试（看命中与相似度评分）
  - 指令：`/kb`（群里打开 = 本群知识库）
  - 容量保护：单个作用域默认最多 400 块（`KB.MAX_TOTAL_CHUNKS`），超出拒绝入库并提示精简
- 👥 **群组用户改为「群列表 → 群成员」两级浏览**：先看所有群（群 ID + 人数），点进去看该群成员，成员点进即场景编辑——群多了也不会糊成一片
- 🚫 **封禁名单**：新增「用户管理 → 🚫 封禁名单」面板（列表 + 一键解封），以及 `/ban <用户ID> [原因]`、`/unban <用户ID>` 指令；封禁是用户级的，私聊与所有群同时生效

### 数据库

- 🗄️ 新增 `kb_docs`（文档）、`kb_chunks`（分块 + 向量）、`kb_sessions`（知识库引导状态，30 分钟过期）三张表，`SCHEMA_VERSION` 递增到 **9**；定时任务新增 `kb_sessions` 兜底清理

### 修复

- 🐛 `services/users.js` 里 `banUserById` / `unbanUserById` 漏导入 `buildUserKey`，会直接抛 `ReferenceError`（被新增测试当场抓出）
- 🐛 「重复解封」检测不到：SQLite 的 `changes` 统计的是命中行数（把 `blocked=0` 再设成 0 也算 1 行），改为先查状态再更新，并区分「用户不存在」与「不在封禁名单」

### 测试与文档

- 🧪 测试 102 → **126 个**：新增 `test/knowledge.test.mjs`（分块、向量编解码、余弦、关键词兜底、入库/检索/停用/删除、容量保护、AI 注入资料、文件上传、群里粘贴正文）与封禁/群组两级浏览的用例；`test/layout.test.mjs` 覆盖新菜单
- 📝 README 新增「📚 知识库（RAG）」章节（使用流程、作用域、技术实现、容量与安全说明）并同步菜单、指令表、数据库表、目录结构

## v2.3.1 — 2026-09-12

### 变更

- 👥 **用户管理改成二级菜单**：主菜单原来直接放「💬 私聊用户 / 👥 群聊用户」两个入口，现在收敛成一个 `👥 用户管理`，点进去再选：
  - 一级：`👑 管理员控制台 → 👥 用户管理`
  - 二级：`💬 私聊用户` / `👥 群组用户` + `🔙 返回主菜单`
  - 用户列表底部的返回按钮改成 `🔙 返回用户管理`，不会一下子跳回主菜单
- 📐 主菜单按钮由 10 个减到 9 个，仍是两列网格：私聊 6 行、群聊 5 行，排版仍在 8 行以内

### 测试与文档

- 🧪 测试 100 → **102 个**：新增「用户管理二级菜单排版」与「点击用户管理进入二级菜单」两个用例，并断言私聊 / 群组入口不再出现在主菜单
- 📝 同步 `README.md`（管理控制台按钮说明）与测试数量；版本号 `2.3.1`

## v2.3.0 — 2026-09-12

### 新增 / 重构

- 📐 **统一的菜单排版工具 `src/utils/layout.js`**：`grid`（两列网格）、`compactLabel`（按码点截断，不切坏 emoji）、`clampPage` / `totalPagesOf` / `pageOffset` / `pagerRow`（分页）、`validateKeyboard`（约束校验）。所有会随数据量增长的菜单都改为调用它，并导出纯函数键盘便于测试
- 📄 **菜单分页补齐**：每日任务列表（每页 6 个）、商城商品列表、订单列表、用户列表都做了页码收敛（越界自动回到最后一页）与行数控制，最多 8 行
- 📂 **商城分类集中到 `src/shop/categories.js`**：原先在 4 个文件里各写一份映射
- 🧩 **限额 / 冷却解析集中到 `services/users.js`**（`parseMaxDaily` / `parseRateLimit`），去掉三处重复实现
- 🗄️ **SQL 拆分逻辑共用**：`splitSchemaStatements` 导出后同时被生产建表与测试用 D1 替身复用，并支持行尾注释

### 修复

- 🐛 **引导式输入会话会一直吞消息**：`shop_add_sessions` / `shop_edit_sessions` / `task_edit_sessions` 缺少有效期，管理员点开后走开，之后的普通私聊消息会被当成表单内容。现在统一 30 分钟过期，定时任务再兜底清理（`task_edit_sessions` 之前完全没人清理）
- 🐛 **群聊指令被同步阻塞 5 秒**：指令处理传入的是复合上下文，`sendAutoDelete` 取不到 `waitUntil`，于是走了 `await sleep(5000)`。现在会解析出真正的 Worker ctx，自动删除稳定放进后台任务
- 🐛 **兑换码过期判断时区不一致**：列表页用 UTC 日期、兑换时用 `APP_TIMEZONE`，凌晨时段会出现「列表说已过期、其实还能用」。统一走 `getDateKey(env)`
- 🐛 **任务列表按钮撑爆菜单**：任务数量由管理员自由增删，原来没有分页；现在每页 6 个 + 翻页
- 🐛 **AI 扣分流水余额可能不准**：改为使用扣分时返回的余额，不再二次查库
- 🐛 **签到加分失败没有回滚**：用户记录异常时会回滚签到记录，避免「显示已签到但没拿到积分」
- 🐛 **群聊指令误删他人 @ 提及**：`/broadcast@Bot @某人` 不再把 `@某人` 一起删掉
- 🐛 **商城限购并发兜底**：下单后按真实订单数复核，并发点击导致超出的那一单自动取消并退款
- 🐛 **引导会话脏草稿会让流程抛异常**：`task_edit_sessions.draft` 解析加了 try/catch
- 🐛 **多处菜单单行 3~4 个按钮**：积分管理、限额、频率、自定义下注、游戏主界面、商品编辑面板统一改为两列
- 🔧 **`Math.random` 违反项目约定**：Telegram 重试抖动改用 `utils/random.js` 的加密随机数

### 测试与文档

- 🧪 测试 79 → **100 个**：新增 `test/regression.test.mjs`（会话过期、群聊不阻塞、签到回滚、边界解析、任务列表翻页、积分夹断），并把 `test/layout.test.mjs` 扩展到覆盖全部动态菜单
- 📝 同步更新 `README.md`（菜单排版约定、目录结构、测试数量）与 `AGENTS.md`（排版与引导会话 TTL 约定）
- 🏷️ 版本号同步到 `package.json` 与 `package-lock.json`

## v2.2.0 — 2026-09-12

### 新增 / 恢复

- ⚙️ **功能开关恢复三级结构**（在 v2.1.0「只保留全局」的基础上，按需求补回场景级控制）：
  - 🌍 全局设置 —— 所有私聊与群聊的默认值
  - 👥 群聊场景 —— 选一个群 → 单独开关（不设置则跟随全局）
  - 💬 私聊场景 —— 选一个用户 → 单独开关
  - 生效顺序：场景显式设置 → 全局设置 → 默认开启；场景页支持「🔄 全部恢复跟随全局」
  - 入口：管理控制台 → ⚙️ 功能开关；场景管理菜单里也有「⚙️ 功能开关」直达
- 📐 **菜单排版优化**（不再拉成一条长条）：
  - 管理员主菜单改为**两列网格**，9 个入口压缩到 6 行
  - 功能开关、每日任务列表、兑换码列表、私聊用户列表统一两列排布（长名字自动截断）
  - 场景管理菜单按钮文案精简（积分 / 流水 / 限额 / 频率 / 功能开关 / 封禁 / 清空记忆 / 删除场景）
  - 用户列表每页 5 → 8 条

### 修复与优化

- 🗄️ 移除 v2.1.0 里那条 `DELETE FROM scene_settings WHERE scene_key <> 'global'` 迁移——它会**在每次冷启动清空场景级开关**；`SCHEMA_VERSION` 递增到 8，并补了回归测试锁住这个行为
- 🧪 测试 71 → **79 个**：新增三级开关（全局/场景覆盖/单个清除/批量恢复）、场景开关不被迁移清除、以及菜单排版（两列、行数上限、按钮文案与 `callback_data` 长度）三类用例

## v2.1.1 — 2026-09-12

### 修复与优化

- 🗄️ **Schema 引导加版本标记**：`src/core/db.js` 新增 `SCHEMA_VERSION` 与 `schema.version` 记录，冷启动时先读版本，已是最新就跳过建表与迁移（原来每次冷启动要跑 42 条语句，D1 对单次调用的查询数有限制，存在隐患）
- 🧪 新增两个用例：版本写入 / 已是最新时跳过建表、冷启动只查一次库（共 **71 个**用例）
- 📄 新增 `AGENTS.md`：记录本仓库的协作约定（不提交密钥、本地部署、提交前自检、Schema 变更流程、`github.com` 不可达时的 API 推送兜底等）

## v2.1.0 — 2026-09-12

### 新增功能

- ✅ **每日任务可引导式增删改**：`/admin → ✅ 每日任务管理`
  - 添加：选触发条件（签到 / 聊天 / 游戏 / 商城 / 兑换码）→ 任务名称 → 完成提示 → 奖励积分，全程引导
  - 修改：改名称 / 改提示 / 改奖励；可停用启用，也可删除（二次确认）
  - 同一个触发条件支持挂多个任务（例如「和 AI 聊一次 +3」再叠加「多聊一句 +2」）
  - **全勤奖可配置**（0 = 不发），改完立刻生效
  - 任务定义由代码里的「触发条件」+ 数据库里的「文案与奖励」组成：机器人只能观测到有限行为，所以触发器仍在代码里，其余交给管理员
- 🗄️ 新增全局设置读写服务 `src/services/settings.js`，功能开关与任务设置统一走它

### 变更

- ⚙️ **功能开关收敛为全局**：移除 v2.0.0 的「单场景覆盖」
  - 入口只剩 `/admin → ⚙️ 功能开关（全局）`，对所有私聊与群聊生效
  - 场景编辑面板里的「🧩 本场景功能开关」按钮已移除，历史遗留的场景级开关记录在首次请求时自动清理
  - 生效顺序由「场景 → 全局 → 默认」简化为「全局设置 → 默认开启」

### 修复与优化

- 🎟️ 使用兑换码也会触发「使用一次兑换码」类任务（新增 `redeem` 触发条件）
- 🗃️ 数据库新增 `daily_task_defs`、`task_edit_sessions` 两张表；默认任务用一次性标记（`task.seeded`）灌入，**管理员删掉的任务不会在下次启动时复活**
- 🔁 旧版按任务键（`checkin` / `chat` / …）记录的完成进度自动迁移为按任务 ID（`t<id>`）记录
- 🔤 修正 `daily_tasks` 与审计日志的新动作文案（任务增删改、启停、全勤奖）
- 🧪 测试 65 → **69 个**：新增任务 CRUD、多任务同触发、删除后进度保留、以及「引导式添加/修改任务」的入口链路测试

## v2.0.0 — 2026-09-12

**主题：从「脚本集合」升级成「可配置的平台」** —— 命令、功能、玩法都能配置，用户侧新增每日任务留存玩法，商城补上防刷能力。

### 架构

- 🧭 **声明式命令注册表**：所有命令集中定义在 `src/handlers/commands/registry.js`（名称 / 别名 / 权限 / 仅私聊 / 归属功能开关 / 说明）
  - `message.js` 不再维护一长串 `if` 分支，只保留消息解析与 AI 兜底
  - `/help` **由注册表自动生成**，并按「普通/管理员」「私聊/群聊」裁剪，杜绝「加了命令忘了写帮助」
  - 新增命令只需加一条注册项，权限、群聊拦截、功能开关、帮助文案全部自动生效

### 新增功能

- ✅ **每日任务**：`/tasks`（别名 `/daily`）
  - 四个任务：签到 / 和 AI 聊一次 / 玩一局游戏 / 在商城兑换一次
  - 完成即刻发奖，四项全部完成再发一次全勤奖（每天一次，并推送祝贺消息）
  - 进度按「用户 + 日期」记录，跨私聊与群聊共用；`/profile` 显示今日进度，管理端统计显示今日完成次数
- ⚙️ **功能开关**：AI 对话 / 游戏大厅 / 每日签到 / 积分商城 / 兑换码 / 每日任务，均可单独开关
  - 全局默认：`/admin → ⚙️ 功能开关（全局默认）`
  - 单场景覆盖：场景编辑 → `🧩 本场景功能开关`，支持「🔄 全部恢复跟随全局」
  - 生效顺序：场景设置 → 全局设置 → 默认开启；指令、按钮回调、AI 回复三处入口都会被拦截
- 🛒 **商品限购**：`shop_items.per_user_limit`（0 = 不限），下单前校验已购数量，**已取消订单不占名额**；商品详情、管理详情与 `/shop_edit → 🙋 改限购` 都可查看/修改

### 修复与优化

- 🗃️ 数据库新增 `scene_settings`、`daily_tasks` 两张表，`shop_items` 新增 `per_user_limit` 列（首次请求自动迁移）
- 🏷️ 补齐 v1.3.0 遗漏的审计日志文案（兑换码生成/启停、功能开关），历史 `shop_order_ship` 记录标注为旧版
- 🧪 测试从 46 个增加到 **65 个**：新增功能开关、每日任务、命令注册表三组用例，入口链路测试补充「限购」与「功能开关拦截」

## v1.3.1 — 2026-09-12

### 变更

- 🚫 **移除「实物商品」分类**：商城现在只支持 **虚拟物品** 与 **服务**；`/shop_add`、`/shop_edit` 的分类选项与校验同步调整（旧数据里的 `physical` 分类会在首次请求时自动归入虚拟物品）
- 🚫 **移除「发货」环节**：订单状态机简化为 `待处理 → 已完成 / 已取消`；管理端的「🚚 标记已发货」改为「✅ 标记已完成（已发放）」，历史 `shipped` 订单自动迁移为已完成
- 🔔 **标记完成会通知用户**：管理员确认发放后，用户会收到「订单已完成」消息（含订单号、商品与备注）
- 🧾 下单备注的定位调整为「服务类商品的补充说明」（想要的款式、联系方式、兑换要求等），能力不变

### 修复与优化

- 🧪 测试更新：新增分类与订单状态的数据迁移测试，商城相关用例改用 `done` 状态，共 46 个用例

## v1.3.0 — 2026-09-12

### 新增功能

- 🎟️ **兑换码**：管理员 `/code_new <积分> [次数] [有效天数]` 生成形如 `TG7KQ2M4XZ` 的码，用户在私聊 `/redeem <码>` 领积分
  - 每个码**每人限兑一次**（数据库唯一约束兜底），可选总次数上限（`0` = 不限）与过期日期
  - `/code_list` 或管理控制台「🎟️ 兑换码」可查看已用/上限并随时停用、启用
- 🧾 **下单备注**：商品详情页新增「✍️ 填写备注」，可填地址、联系方式等；备注随订单写入 `remark`，同时出现在管理员的新订单通知和订单详情中
- ⏰ **定时任务（Cron Triggers）**：每天自动清理过期管理员会话、陈旧群发/引导/备注草稿，停用过期兑换码，并给管理员推送「昨日概况 + 最早几笔待处理订单」日报（带快捷按钮）
- 🧪 **测试套件**：`npm test`（`node:test` + `node:sqlite` 内存库跑真实 SQL），43 个用例覆盖表结构与迁移、连续签到、积分边界、兑换码全状态、商城取消退款幂等、下单备注、定时任务清理与统计、AI 上下文裁剪、入口链路
- 🧹 `npm run check` 现在也会检查 `test/`、`test-helpers/` 的语法与 import 路径

### 修复与优化

- 🧠 AI 上下文裁剪从 `handlers/ai.js` 抽成 `src/services/history.js`，逻辑不变但可单测
- 🗂️ 备注输入期间发送指令不会被误当成备注内容，只有 `/cancel` 会放弃填写；进入输入态 30 分钟未回复会自动失效，避免正常聊天被吞成备注；下单失败时草稿保留，用户不用重填
- 🗃️ 数据库新增 `redeem_codes`、`redeem_logs`、`shop_order_drafts` 表（首次请求自动创建，老库无需手工 SQL）
- ⚙️ `wrangler.toml` 增加 `[triggers] crons = ["0 16 * * *"]`（UTC，等于北京时间 00:00）

## v1.2.0 — 2026-09-12

### 新增功能

- ✏️ **管理员引导式编辑商品**：`/shop_edit <商品ID>` 或「商品详情 → ✏️ 编辑」，可改名称、价格、库存、分类、图标、说明
- ❌ **用户自助取消订单**：`/orders` 与商城「我的订单」里，待处理订单可一键取消并退款（库存同步回滚）
- 📜 **积分流水分页**：`/points` 支持上一页 / 下一页翻页
- 🏆 **积分排行榜**：`/rank` 显示积分 Top 10，并显示自己的排名
- 📅 **连续签到递增奖励**：首日 +5，逐日 +1 上限 +20，每满 7 天额外 +20，断签重置
- 📢 **管理员群发**：`/broadcast <内容>` 二次确认后分批推送给所有私聊用户，支持断点续发
- 🚫 **封禁 / 解封用户**：被封禁用户无法使用 AI、游戏、商城等任何功能，管理员可在场景编辑面板切换
- 📋 **管理员操作日志**：记录谁在什么时候改了什么，控制台可翻页审计
- 🧹 **记忆管理**：`/clearmem <群ID> [用户ID]` 或场景编辑面板「清空此场景记忆」，支持按「群 + 用户」清理
- 💾 **D1 备份脚本**：`npm run backup` / `npm run backup:local`，输出到 `backups/`，附定时备份方案
- 🔐 **生产配置私有备份**：`npm run backup:config` 通过 GitHub API 把 `wrangler.production.toml` 与 `.dev.vars` 备份到私有仓库，上传前会校验目标仓库必须为私有
- 🤖 **AI 模型自动回退**：主模型失败或返回空内容时自动切换备选模型
- 🧮 **AI 上下文长度管理**：历史按字符预算（默认 6000）截断，单条消息也会截断
- 🔁 **Telegram 429 退避重试**：遇到限流 / 5xx / 网络抖动自动退避重试（最多 3 次）
- ✅ **本地自检脚本**：`npm run check` 检查全部文件语法与相对 import 路径
- ⬆️ **wrangler 升级到 4.x**：修复依赖漏洞告警，部署产物与 3.x 一致
- 🌍 **代码与生产配置分离**：公开仓库只保留代码与占位符模板（无机器人用户名 / 账号 ID / 密钥），真实配置留在本地被 gitignore 的 `wrangler.production.toml`

### 修复与优化

- 🧹 **移除 GitHub Actions 工作流**（自动部署 + 代码自检）：本仓库定位是「代码备份 + 开源」，部署统一在本地用 `npm run deploy:prod` 完成，提交前用 `npm run check` 自检；不在公开仓库里挂部署凭据
- 📄 许可信息统一为 **GPL-3.0**：仓库根目录的 `LICENSE` 一直是完整的 GPL-3.0 全文，README 的「许可」章节此前误写为 MIT，现已改正并在 `package.json` 补上 `"license": "GPL-3.0-or-later"`（第 3 版或更高版本）
- 📖 重写 README：重排章节结构，补齐新指令、配置项、数据库表、备份/安全与常见问题
- 🔒 配置备份脚本改为从本地未跟踪文件 `.config-backup` 读取目标仓库，公开仓库里不再出现私有仓库名
- 🧾 订单取消改为原子状态流转（`WHERE status = 'pending'`），避免并发下重复退款
- 🗂️ 商城模块新增 `shop/add.js`、`shop/edit.js`，用户与管理员共用同一套退款逻辑
- 🗃️ 数据库新增 `shop_edit_sessions`、`admin_logs`、`broadcast_drafts` 表与 `users.blocked` 字段（自动迁移）
- 📄 `/orders` 与商城订单页合并为同一套渲染逻辑，消除重复代码
- 🧠 AI 请求数组不再被后续流程改写，落库历史与请求内容解耦

## v1.1.0 — 2026-09-12

### 新增功能

- ➕ 管理员引导式添加商品
- 🔐 新增可选 `WEBHOOK_SECRET` webhook 安全校验
- 📄 商城商品分页
- 📜 用户可通过 `/points` 查看自己的积分流水
- ⌨️ 注册 Telegram 指令列表，输入 `/` 显示指令提示

### 修复与优化

- 🔗 启用 Workers AI 与 D1 绑定，修复 AI 未绑定报错
- 🔒 订单商品名 HTML 转义，防止特殊字符破坏消息
- 🧾 管理员调整积分时，流水记录实际变化量
- 🎯 群聊指令目标识别，自动忽略发给其他机器人的指令
- 🖼️ 欢迎页与个人卡片排版优化，管理员/普通用户差异化展示
- 🐛 修复 Telegram 指令描述中文显示为 `???` 的问题

## v1.0.0 — 2026-09-12

首个版本：Telegram AI Bot 上线 Cloudflare Workers。

### 功能

- 🤖 AI 对话（Cloudflare Workers AI，Llama 3.3 70B）
- 🪙 全局积分系统与每日签到
- 🎮 游戏大厅：骰子猜大小、欢乐老虎机、抛硬币、幸运转盘
- 🛒 积分商城：商品浏览、兑换、我的订单、库存管理
- 👑 管理控制台：用户/场景管理、积分调整、每日限额与发送频率设置
